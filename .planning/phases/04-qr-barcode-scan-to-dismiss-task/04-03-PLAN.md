---
phase: 04-qr-barcode-scan-to-dismiss-task
plan: 03
type: execute
wave: 2
depends_on: ["04-01"]
files_modified:
  - lib/alarm/screens/alarm_notification_screen.dart
files_modified_note: "TEMPORARY throwaway scaffold only — this edit is reverted before the plan completes; it does not co-own this file with any other plan"
autonomous: false
requirements: []
requirements_note: "Criterion-#1 de-risk spike — informs SCAN-03/SCAN-07 lock-screen UX path (D-LOCK-NOGO-UX); no requirement is source-completed here"

must_haves:
  truths:
    - "A documented go / no-go / requires-unlock decision exists for a live flutter_zxing ReaderWidget preview over a SECURE (PIN/pattern) keyguard, on at least two OEMs"
    - "The spike reuses the REAL ring path (FlutterShowWhenLocked().show()) — not a normal in-app screen"
    - "The throwaway scaffold is fully reverted; no spike code remains in the committed tree"
  artifacts:
    - path: ".planning/phases/04-qr-barcode-scan-to-dismiss-task/04-LOCKSCREEN-SPIKE.md"
      provides: "Per-OEM decision matrix + go/no-go verdict"
      contains: "Verdict"
  key_links:
    - from: "spike ReaderWidget"
      to: "FlutterShowWhenLocked().show() over-lock window"
      via: "rendered inside AlarmNotificationScreen on a fired alarm over a secure keyguard"
      pattern: "ReaderWidget"
---

<objective>
Resolve the milestone's single biggest unknown (ROADMAP success criterion #1): does a live `flutter_zxing` `ReaderWidget` camera preview RENDER and DECODE inside Chrono's existing `showWhenLocked` over-lock window — opened by the runtime call `FlutterShowWhenLocked().show()` (`alarm_notifications.dart:182`) — over a SECURE (PIN/pattern) keyguard, across at least two distinct OEMs? Produce a documented go / no-go / "requires unlock first" decision BEFORE the scan-task UI is committed (D-LOCK-SPIKE-SCOPE).

Purpose: No document can answer this — it is inherently an on-device hardware spike (some OEMs blank camera surfaces over a secure keyguard for privacy). The result decides which PRIMARY lock-screen path each device class gets, NOT whether to ship: per D-LOCK-SHIP the feature ships regardless because the default-on escape hatch is the universal safety net, and no-go OEMs degrade to unlock-then-scan (D-LOCK-NOGO-UX). This plan does NOT build the real task UI — it adds a throwaway scaffold, observes, decides, and reverts.

Output: `.planning/phases/04-qr-barcode-scan-to-dismiss-task/04-LOCKSCREEN-SPIKE.md` (the per-OEM decision matrix + verdict), and a fully-reverted `alarm_notification_screen.dart`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-qr-barcode-scan-to-dismiss-task/04-RESEARCH.md
@.planning/phases/04-qr-barcode-scan-to-dismiss-task/04-01-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add a throwaway ReaderWidget spike scaffold on the real over-lock ring path</name>
  <files>lib/alarm/screens/alarm_notification_screen.dart</files>
  <read_first>
    - lib/alarm/screens/alarm_notification_screen.dart (the `_setNextWidget()` ring state machine, lines ~41-63, and the `actionWidget`/`_currentWidget` slot — this is where the bare ReaderWidget is temporarily injected; do NOT change `_setNextWidget`'s real logic, only insert the throwaway debug preview)
    - lib/notifications/logic/alarm_notifications.dart (line ~182 `FlutterShowWhenLocked().show()` — confirm the over-lock window the spike must reuse; do not modify)
    - .planning/phases/04-qr-barcode-scan-to-dismiss-task/04-RESEARCH.md "The Lock-Screen Camera Spike (Item 1)" (the minimal spike scaffold steps + why an in-app screen does NOT reproduce the keyguard condition)
    - .planning/phases/04-qr-barcode-scan-to-dismiss-task/04-RESEARCH.md "Pattern 3: ReaderWidget result handling" (the ReaderWidget onScan/onControllerCreated signatures)
  </read_first>
  <action>
    Add a TEMPORARY, clearly-marked throwaway debug scaffold inside `AlarmNotificationScreen` so that, on a real fired alarm (the genuine over-lock path via the existing notification → `FlutterShowWhenLocked().show()` window), the screen renders a bare `ReaderWidget(onScan: (c) => debugPrint('DECODED'))` in place of (or above) the normal action widget. Reusing the REAL ring path is essential — a normal in-app screen will NOT reproduce the secure-keyguard condition (RESEARCH Item 1). Gate the scaffold behind an obvious `// SPIKE: REMOVE BEFORE COMMIT` marker and a single boolean constant so it is trivially excised. Use `onControllerCreated: (controller, exception) => debugPrint('CONTROLLER exception=$exception')` to capture camera-init failures. Do NOT log the decoded payload value — `debugPrint('DECODED')` (no `c.text`) — privacy holds even in the spike.
    This is throwaway: do NOT build the real `ScanTask`, schema, or registration here — those are Plans 04/05. Do NOT alter `_setNextWidget`'s real task-iteration logic. The scaffold exists only to drive a `ReaderWidget` through the real `FlutterShowWhenLocked` window.
    The native build is required to compile the ReaderWidget — that build runs in CI (`test-apk.yml`, the dev APK from Plan 01's toolchain); the sideloadable dev APK is what gets installed on the test devices. Toolchain absent locally: do NOT claim a local build.
  </action>
  <verify>
    <human-check>The dev APK (built by test-apk.yml) installs and, on a fired alarm over a SECURE keyguard, surfaces a ReaderWidget preview surface. The scaffold is behind a single removable boolean + `// SPIKE` markers.</human-check>
  </verify>
  <acceptance_criteria>
    - The temporary `ReaderWidget` is rendered on the REAL ring path (inside `AlarmNotificationScreen`, reachable from a fired alarm), NOT a normal in-app debug screen
    - The scaffold is gated behind a single clearly-marked boolean constant + `// SPIKE: REMOVE BEFORE COMMIT` comments (trivially excisable)
    - `onScan` logs `'DECODED'` with NO payload value; `onControllerCreated` logs the init exception
    - `_setNextWidget`'s real task-iteration logic is unchanged
    - No `ScanTask`/schema/registration code authored in this task (deferred to Plans 04/05)
  </acceptance_criteria>
  <done>A removable ReaderWidget spike scaffold drives the camera through the real FlutterShowWhenLocked over-lock window; payload never logged; real ring logic untouched.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: On-device lock-screen camera spike — decision matrix across >=2 OEMs</name>
  <files>.planning/phases/04-qr-barcode-scan-to-dismiss-task/04-LOCKSCREEN-SPIKE.md, lib/alarm/screens/alarm_notification_screen.dart</files>
  <action>
    HUMAN CHECKPOINT — no autonomous code change. Claude pauses; the human performs the on-device spike in <how-to-verify> below and gives the resume-signal. Claude must NOT proceed past this gate until the human confirms the verdict AND the scaffold reversion. This gate is never auto-approvable (on-device only).
  </action>
  <what-built>
    A throwaway ReaderWidget preview wired onto the real over-lock ring path (Task 1), shipped in the dev APK (built by test-apk.yml). This checkpoint runs the actual hardware spike and records the go/no-go decision that shapes the lock-screen UX path (D-LOCK-NOGO-UX) for Plan 05 / a future ring-time lock-screen branch.
  </what-built>
  <how-to-verify>
    For EACH of at least two distinct-OEM physical devices (recommended: a Pixel/AOSP device + a Samsung OneUI device; add Xiaomi/MIUI if available — MIUI is the most aggressive over-lock restrictor and surfaced flutter_zxing issue #114):
    1. Set a SECURE keyguard (PIN or pattern — not swipe/none) and lock the device.
    2. Schedule an alarm a minute out; let it fire so the real `FlutterShowWhenLocked().show()` over-lock window opens with the spike ReaderWidget.
    3. Point the camera at a known-good test QR/barcode and observe, recording per device:
       - Does a LIVE preview render (frames moving), or is it black/blank/frozen?
       - Does `onScan` fire ('DECODED' in logcat) on the test code?
    4. Classify each device using the RESEARCH decision matrix:
       - Live preview AND onScan decodes → GO (build direct over-lock scan on the showWhenLocked path)
       - Black/blank/frozen preview → NO-GO → unlock-then-scan fallback (D-LOCK-NOGO-UX)
       - Preview renders but onScan never fires (cf. #114) → REQUIRES-INVESTIGATION → treat as no-go for that OEM (escape hatch covers it)
    5. Write `.planning/phases/04-qr-barcode-scan-to-dismiss-task/04-LOCKSCREEN-SPIKE.md` with one row per device (OEM, Android version, skin, preview?, decode?, verdict) and an overall verdict + which path each device class gets. Per D-LOCK-SHIP the feature ships regardless — the verdict only decides PRIMARY path, never whether to ship.
    6. REVERT the throwaway scaffold from `alarm_notification_screen.dart` (remove the SPIKE-gated block; restore the file to its pre-spike state) and confirm the file diff is clean.
  </how-to-verify>
  <verify>
    <human-check>`04-LOCKSCREEN-SPIKE.md` exists with >=2 OEM rows (preview?/decode?/verdict) + an overall per-class verdict, AND `git diff lib/alarm/screens/alarm_notification_screen.dart` is empty (scaffold reverted).</human-check>
  </verify>
  <done>A documented go/no-go/requires-unlock verdict per device exists in 04-LOCKSCREEN-SPIKE.md; the throwaway scaffold is fully reverted; the verdict feeds the lock-screen UX path (direct scan on go, "unlock to scan" on no-go).</done>
  <resume-signal>Type "go", "no-go", or "mixed: <per-OEM>" and confirm the spike scaffold has been reverted. Describe any device that rendered but did not decode.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| camera surface ↔ secure keyguard | The OS may blank a camera preview composited over a secure keyguard for privacy — the exact behavior the spike measures |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-07 | Information Disclosure | spike debugPrint | mitigate | Spike logs `'DECODED'` only, never `code.text` — privacy holds even in throwaway code |
| T-04-08 | Denial of Service (against the user) | no-go OEM camera over keyguard | accept (mitigated downstream) | A no-go device cannot scan over the keyguard, but the default-on escape hatch + unlock-then-scan fallback (D-LOCK-NOGO-UX) guarantee dismissability — ship-regardless per D-LOCK-SHIP |
</threat_model>

<verification>
- Criterion #1: a documented go/no-go/requires-unlock decision exists per device in `04-LOCKSCREEN-SPIKE.md`, derived on the REAL over-lock path across >=2 OEMs.
- The throwaway scaffold is reverted — `git diff` of `alarm_notification_screen.dart` is empty after the checkpoint.
- This is an inherently on-device gate (autonomous:false); CI cannot reproduce secure-keyguard + real camera (RESEARCH: do not add an emulator job).
</verification>

<success_criteria>
- `04-LOCKSCREEN-SPIKE.md` records >=2 OEM rows with preview/decode observations and a per-class verdict.
- The verdict feeds the Plan-05 lock-screen UX path (direct scan on go; "unlock to scan" prompt on no-go).
- The spike scaffold is fully reverted; no spike code is committed.
</success_criteria>

<output>
Create `.planning/phases/04-qr-barcode-scan-to-dismiss-task/04-03-SUMMARY.md` when done. Record the per-OEM verdict, which path each device class gets, and confirm the scaffold reversion. Append the on-device spike result to `.planning/MANUAL-VERIFICATION-LOG.md` (per the manual-verification-log memory).
</output>
