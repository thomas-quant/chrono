---
phase: 04-qr-barcode-scan-to-dismiss-task
plan: 06
type: execute
wave: 4
depends_on: ["04-03", "04-04", "04-05"]
files_modified: []
files_modified_note: "On-device verification only — no source files changed; any defects found are filed as gap-closure plans (--gaps), not fixed inline here"
autonomous: false
requirements: [SCAN-09, SCAN-11]
requirements_note: "SCAN-09 (torch) and SCAN-11 (camera release) have their behavioral confirmation here; SCAN-01..08/10/12 are source-complete in Plans 04/05 and CI — this plan is their on-device sign-off"

must_haves:
  truths:
    - "On a real fired alarm, the scan task opens a live camera and dismisses ONLY when the registered code is scanned"
    - "Scanning a wrong code does not dismiss; after the escape threshold (or instantly on camera-denied/unavailable) a screen-reader-reachable Dismiss appears"
    - "Snooze works as a normal pre-task swipe without entering the scanner"
    - "The torch toggle works in a dark room (or degrades gracefully on a no-flash device)"
    - "The camera/privacy indicator clears on every exit path (match, escape, background) — no stuck indicator"
    - "On no-go OEMs (per the spike) the 'unlock to scan' degradation behaves correctly and the escape hatch still guarantees dismissability"
  artifacts:
    - path: ".planning/MANUAL-VERIFICATION-LOG.md"
      provides: "Appended phase-4 on-device verification results"
      contains: "Phase 4"
  key_links:
    - from: "registered code"
      to: "ring-time dismiss"
      via: "real scan of the registered code over the fired-alarm screen"
      pattern: "scan"
---

<objective>
Sign off the on-device behaviors CI genuinely cannot exercise: the real ring-time scan-to-dismiss flow (live `flutter_zxing` camera over a fired alarm), wrong-scan feedback + escape-hatch surfacing, snooze-without-scanner (SCAN-05), the torch toggle (SCAN-09), camera release on every exit path / no stuck privacy indicator (SCAN-11), and the no-go-OEM "unlock to scan" degradation (D-LOCK-NOGO-UX) informed by the Plan-03 spike. This is the phase's human gate — everything authorable in CI was landed in Plans 01-05; this plan confirms the assembled feature works on real hardware.

Purpose: Per CLAUDE.md the only legitimate on-device-only gates are real camera, real alarm firing, lock-screen, and camera-release — none reproducible in CI (no emulator/integration_test job, by RESEARCH recommendation). This plan does NOT write source; if a defect is found it is filed as a gap-closure plan (`/gsd-plan-phase --gaps`), not patched inline. Confirms SCAN-09 and SCAN-11 behaviorally and signs off the assembled SCAN-01..08/10/12 flow.

Output: appended results in `.planning/MANUAL-VERIFICATION-LOG.md` and a go/defect verdict.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-qr-barcode-scan-to-dismiss-task/04-RESEARCH.md
@.planning/phases/04-qr-barcode-scan-to-dismiss-task/04-UI-SPEC.md
@.planning/phases/04-qr-barcode-scan-to-dismiss-task/04-03-SUMMARY.md
@.planning/phases/04-qr-barcode-scan-to-dismiss-task/04-04-SUMMARY.md
@.planning/phases/04-qr-barcode-scan-to-dismiss-task/04-05-SUMMARY.md
</context>

<tasks>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 1: On-device end-to-end scan-task verification (SCAN-09/11 + assembled flow)</name>
  <files>.planning/MANUAL-VERIFICATION-LOG.md</files>
  <action>
    HUMAN CHECKPOINT — no autonomous code change. Claude pauses; the human performs the on-device verification matrix in <how-to-verify> below after confirming the CI prerequisites are green, then gives the resume-signal. Claude must NOT proceed (and the phase is NOT closed) until the human confirms the result. If any check fails, Claude does not patch inline — it records the failure for `/gsd-plan-phase --gaps`. This gate is never auto-approvable (on-device only).
  </action>
  <what-built>
    The complete scan-to-dismiss task assembled across Plans 01-05: a `Scan code to dismiss` alarm task (Plan 04) with an inline registration card + setup-time camera permission (Plan 05), a ring-time `ScanTask` that dismisses only on a matching registered code, fires the escape hatch on threshold/camera-failure, and exposes a torch + Semantics Dismiss. The CI gates (pure-seam tests, JSON round-trip, BUILD-02 zero-ML-Kit graph, native build) are confirmed green in CI BEFORE running this checkpoint. The Plan-03 spike verdict tells you which lock-screen path each test device gets.
  </what-built>
  <how-to-verify>
    PREREQUISITE: confirm in CI (do NOT run locally — toolchain absent) that `tests.yml` (`flutter test`, incl. code_match / escape_hatch_controller / alarm_task_scan tests) is green, the `test-apk.yml` dev-APK native build compiles the flutter_zxing native libs, and the BUILD-02 prod-release dependency-graph gate PASSED (zero mlkit/gms/play-services). Install the resulting dev APK on the test devices.

    Then on a real device (run the full matrix on >=1 go device and, if the spike found any, >=1 no-go device):
    1. **Register (SCAN-02/08/10):** Create an alarm, add the `Scan code to dismiss` task. Tap the inline "Scan to register" card → confirm camera permission is requested AT SETUP (not at fire time). Grant it; scan a QR or a 1D barcode (test EAN/UPC/Code128 too — SCAN-04). Confirm the card now shows "✓ Code registered" (status only — the raw value is NEVER shown). Deny permission on a second attempt and confirm the deep-link to system settings appears, then resumes.
    2. **Match → dismiss (SCAN-03/05):** Let the alarm fire. Swipe to dismiss → the full-screen scanner opens. Scan the REGISTERED code → the alarm dismisses. Confirm snooze worked as a normal pre-task swipe WITHOUT entering the scanner (SCAN-05). Confirm a trailing-newline/case variant of the same code still dismisses (normalization, SCAN-03).
    3. **Wrong scan + escape (SCAN-06/07):** Fire again; scan a DIFFERENT code → confirm wrong-scan haptic + transient feedback, no dismiss. Wait out the ~120s time threshold OR scan wrong ~10 times → confirm a Dismiss button appears and is reachable by TalkBack (screen-reader). Separately, deny/disable the camera and confirm the escape Dismiss appears INSTANTLY (SCAN-07).
    4. **Torch (SCAN-09):** In a dark room toggle the flashlight on/off; on a no-flash device confirm graceful "Couldn't turn on the flashlight" handling, scanner keeps running.
    5. **Camera release (SCAN-11):** After EACH exit (match-dismiss, escape-dismiss, sending the app to background mid-scan) confirm the OS camera/privacy indicator CLEARS (green dot gone) and a subsequent alarm's scanner can acquire the camera (no "camera in use").
    6. **No-go OEM degradation (D-LOCK-NOGO-UX):** On any device the spike flagged no-go, confirm the "unlock to scan" prompt shows over the keyguard, the alarm keeps ringing until unlocked, and the escape hatch still fires underneath (never trapped).
    7. Append all results (per device: register OK?, match-dismiss OK?, wrong-scan+escape OK?, instant-escape-on-cam-fail OK?, torch OK?, camera-released OK?, no-go degradation OK?) to `.planning/MANUAL-VERIFICATION-LOG.md` under a "Phase 4 — Scan-to-dismiss on-device verification" heading.

    If any check FAILS, do NOT patch inline — record the failure and the orchestrator routes it to `/gsd-plan-phase --gaps`.
  </how-to-verify>
  <verify>
    <human-check>`.planning/MANUAL-VERIFICATION-LOG.md` has a "Phase 4 — Scan-to-dismiss on-device verification" section with a per-device pass/fail matrix covering register, match-dismiss, wrong-scan+escape, instant-escape-on-cam-fail, torch, camera-released, and no-go degradation; CI prerequisites confirmed green.</human-check>
  </verify>
  <done>The assembled scan-to-dismiss flow is confirmed on the device matrix (register-at-setup, dismiss-only-on-registered-code, escape-never-traps, torch works/degrades, camera always released); results logged; any defect filed for gap-closure.</done>
  <resume-signal>Type "verified" (all checks pass on the matrix) or list the failing checks per device for gap-closure.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| camera ↔ OS privacy indicator | The OS privacy indicator reflects whether the camera is actually released on exit — the SCAN-11 observable |
| fired alarm ↔ user dismiss gate | The assembled dismiss gate must never trap the user on any device |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-18 | Information Disclosure | stuck camera/privacy indicator | mitigate | On-device confirmation that the indicator clears on every exit path (SCAN-11); a failure routes to gap-closure |
| T-04-19 | Denial of Service (against the user) | un-dismissable alarm on a real device | mitigate | On-device confirmation that the escape Dismiss appears (threshold or instant on cam-fail) and is TalkBack-reachable across the device matrix (SCAN-06/07) |
</threat_model>

<verification>
- SCAN-09: torch toggles on a real device; graceful no-flash degradation confirmed.
- SCAN-11: camera/privacy indicator clears on match, escape, and background exits; next scanner acquires the camera.
- Assembled SCAN-01..08/10/12: register-at-setup, match-to-dismiss, normalization, wrong-scan+escape, instant-escape-on-cam-fail, snooze-without-scanner, no-go degradation all confirmed on hardware.
- CI prerequisites (pure-seam + JSON tests green, native build compiles, BUILD-02 zero-ML-Kit PASS) confirmed in CI before the checkpoint — never claimed locally.
- Inherently on-device (autonomous:false); no emulator job (RESEARCH recommendation).
</verification>

<success_criteria>
- The full scan-to-dismiss flow works on the device matrix: register at setup, dismiss only on the registered code, escape hatch never traps, torch works/degrades, camera always released.
- Results appended to MANUAL-VERIFICATION-LOG.md.
- Any defect is filed for gap-closure rather than patched inline.
</success_criteria>

<output>
Create `.planning/phases/04-qr-barcode-scan-to-dismiss-task/04-06-SUMMARY.md` when done. Record the per-device verification matrix, the CI-prerequisite confirmation, and any defects routed to gap-closure. Append to `.planning/MANUAL-VERIFICATION-LOG.md`.
</output>
