---
phase: 04-qr-barcode-scan-to-dismiss-task
plan: 04
type: execute
wave: 2
depends_on: ["04-01", "04-02"]
files_modified:
  - lib/alarm/types/alarm_task.dart
  - lib/alarm/data/alarm_task_schemas.dart
  - lib/alarm/widgets/tasks/scan_task.dart
  - lib/l10n/app_en.arb
  - test/alarm/types/alarm_task_scan_test.dart
autonomous: true
requirements: [SCAN-01, SCAN-03, SCAN-04, SCAN-05, SCAN-06, SCAN-07, SCAN-09, SCAN-11, SCAN-12]

must_haves:
  truths:
    - "A user can add a 'Scan code to dismiss' task to an alarm alongside math/retype/sequence/memory"
    - "At ring time the scan task renders a live ReaderWidget full-screen in the dismiss step and dismisses only on a matching registered code"
    - "A non-matching scan gives haptic+visual feedback and counts toward the escape threshold"
    - "Camera-permission-denied / camera-unavailable fires the escape hatch instantly; the Dismiss affordance is screen-reader reachable"
    - "When the camera preview fails to start at ring time (dead/black preview or controller init error), ScanTask renders the Surface-4 'unlock to scan' prompt (scanUnlockToScanTitle/scanUnlockToScanBody) instead of a dead scanner; the alarm keeps ringing and the escape hatch keeps running underneath (D-LOCK-NOGO-UX / SCAN-07)"
    - "The torch toggle is available; the camera is released on every exit path"
    - "Snooze is never inside the scanner (it stays the pre-task swipe action)"
    - "An AlarmTask(scan) round-trips through toJson/fromJson"
    - "All new ring-side user-facing strings are localized in app_en.arb"
  artifacts:
    - path: "lib/alarm/types/alarm_task.dart"
      provides: "AlarmTaskType.scan enum value"
      contains: "scan"
    - path: "lib/alarm/data/alarm_task_schemas.dart"
      provides: "scan schema (Registered Code StringSetting isVisual:false + Escape Hatch SwitchSetting default true) + ScanTask builder"
      contains: "AlarmTaskType.scan"
    - path: "lib/alarm/widgets/tasks/scan_task.dart"
      provides: "ScanTask ring widget (ReaderWidget + match→onSolve + escape + torch + dispose + camera-failure→Surface-4 unlock-to-scan prompt)"
      contains: "class ScanTask"
    - path: "lib/l10n/app_en.arb"
      provides: "all new scan strings (SCAN-12)"
      contains: "scanTask"
    - path: "test/alarm/types/alarm_task_scan_test.dart"
      provides: "AlarmTask(scan) JSON round-trip + schema-present coverage (SCAN-01)"
      contains: "AlarmTaskType.scan"
  key_links:
    - from: "lib/alarm/data/alarm_task_schemas.dart"
      to: "lib/alarm/widgets/tasks/scan_task.dart"
      via: "schema builder returns ScanTask(onSolve, settings)"
      pattern: "ScanTask\\("
    - from: "lib/alarm/widgets/tasks/scan_task.dart"
      to: "lib/alarm/logic/code_match.dart + escape_hatch_controller.dart"
      via: "codesMatch(normalizeCode(...)) gates onSolve; EscapeHatchController gates the Dismiss affordance"
      pattern: "codesMatch|EscapeHatchController"
    - from: "ScanTask.onScan match"
      to: "onSolve()"
      via: "matching registered code advances/dismisses the task"
      pattern: "onSolve\\(\\)"
    - from: "ScanTask camera-preview-failure (device-state signal)"
      to: "Surface-4 unlock-to-scan prompt (scanUnlockToScanTitle/scanUnlockToScanBody)"
      via: "onControllerCreated(_, exception != null) OR a dead-preview flag → render the unlock prompt AND fire/keep the escape hatch; this is a runtime DEVICE-STATE branch, NOT a per-OEM lookup — the Plan 04-03 spike verdict only updates the documented EXPECTED DEFAULT (go OEMs: preview renders; no-go OEMs: unlock-to-scan is the norm), it is never wired into a runtime per-device switch"
      pattern: "scanUnlockToScanTitle"
---

<objective>
Build the ring-time half of the scan task: register `AlarmTaskType.scan` (SCAN-01) with a schema (`Registered Code` StringSetting, hidden raw value per D-REG-DISPLAY, + `Escape Hatch` SwitchSetting default ON per SCAN-06), author the `ScanTask` ring widget that renders a live `ReaderWidget` full-screen in the dismiss step and dismisses only on a matching registered code (SCAN-03/04), wires the escape hatch (SCAN-06/07), torch (SCAN-09) and camera release on every exit (SCAN-11), keeps snooze out of the scanner (SCAN-05), builds the Surface-4 "unlock to scan" degradation for camera-preview-failure at ring time (D-LOCK-NOGO-UX), add all ring-side ARB strings (SCAN-12), and prove `AlarmTask(scan)` JSON round-trips (SCAN-01) in CI.

Purpose: This is the drop-in task — `_setNextWidget()` in `alarm_notification_screen.dart` is type-agnostic (line-confirmed), so the new task is auto-picked-up with ZERO ring-orchestration change (do NOT edit that file). The widget consumes Plan 02's pure seams (`codesMatch`/`normalizeCode`, `EscapeHatchController`) and Plan 01's `flutter_zxing`. Implements SCAN-01, SCAN-03, SCAN-04, SCAN-05, SCAN-06, SCAN-07, SCAN-09, SCAN-11, SCAN-12 (ring side). The setup/registration side (SCAN-02, SCAN-08, SCAN-10) is Plan 05.

Output: edited `alarm_task.dart` (enum), `alarm_task_schemas.dart` (schema), new `scan_task.dart`, edited `app_en.arb` (ALL phase strings), new `alarm_task_scan_test.dart`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-qr-barcode-scan-to-dismiss-task/04-RESEARCH.md
@.planning/phases/04-qr-barcode-scan-to-dismiss-task/04-PATTERNS.md
@.planning/phases/04-qr-barcode-scan-to-dismiss-task/04-UI-SPEC.md
@.planning/phases/04-qr-barcode-scan-to-dismiss-task/04-02-SUMMARY.md
@lib/alarm/types/alarm_task.dart
@lib/alarm/data/alarm_task_schemas.dart
@lib/alarm/widgets/tasks/math_task.dart
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add AlarmTaskType.scan + schema entry + all phase ARB strings</name>
  <files>lib/alarm/types/alarm_task.dart, lib/alarm/data/alarm_task_schemas.dart, lib/l10n/app_en.arb</files>
  <read_first>
    - lib/alarm/types/alarm_task.dart (the enum at lines 8-14 is `math, retype, sequence, shake, memory` — append `scan`; the JSON round-trip at lines 66-77/100-108 is fully generic, no other edit needed; `AlarmTaskBuilder` typedef line 16-17)
    - lib/alarm/data/alarm_task_schemas.dart (the `AlarmTaskType.math` entry, lines 11-47, is the exact shape to mirror: `(getLocalizedName, SettingGroup, builder)` triple; `SwitchSetting`/`StringSetting` come from the existing `package:clock_app/settings/types/setting.dart` import line 6)
    - lib/l10n/app_en.arb (the `mathTask` key pair pattern — `"key": "value"` + `"@key": {}`; the existing `dismissAlarmButton` key to REUSE for the escape Dismiss button — do NOT duplicate it)
    - .planning/phases/04-qr-barcode-scan-to-dismiss-task/04-PATTERNS.md "alarm_task_schemas.dart" + "Non-Dart edits → lib/l10n/app_en.arb" (the full ARB key list)
    - .planning/phases/04-qr-barcode-scan-to-dismiss-task/04-UI-SPEC.md "Copywriting Contract" (every key + its sentence-case value)
  </read_first>
  <action>
    In `lib/alarm/types/alarm_task.dart` append `scan` to the `AlarmTaskType` enum (after `memory`). No other edit — `AlarmTask.fromJson` resolves `AlarmTaskType.values.byName(...)` and the schema generically; an additive enum value needs NO `alarmSchemaVersion` bump (currently 5; RESEARCH Runtime State Inventory confirms additive types default-construct).
    In `lib/alarm/data/alarm_task_schemas.dart` add the `AlarmTaskType.scan` schema entry mirroring the math entry: `getLocalizedName` → `scanTask`; a `SettingGroup("Scan Settings", ...)` containing (a) `StringSetting("Registered Code", ...scanRegisteredCodeTitle, "", isVisual: false)` — `isVisual:false` keeps the raw value hidden / un-rendered (D-REG-DISPLAY, privacy) and the registration card (Plan 05) is what the user sees; (b) `SwitchSetting("Escape Hatch", ...scanEscapeHatch, true)` — DEFAULT true (SCAN-06), with `getDescription` → `scanEscapeHatchDescription`; builder `(onSolve, settings) => ScanTask(onSolve: onSolve, settings: settings)`. Add `import 'package:clock_app/alarm/widgets/tasks/scan_task.dart';`.
    In `lib/l10n/app_en.arb` add ALL phase strings (this plan OWNS the ARB file so Plan 05 does not touch it) using the `"key":"value"` + `"@key":{}` pattern, sentence-case values from UI-SPEC Copywriting Contract: `scanTask`, `scanRegisteredCodeTitle`, `scanRegisterButton`, `scanRescanButton`, `scanCodeRegistered`, `scanNoCodeRegistered`, `scanCodeRequired`, `scanEscapeHatch`, `scanEscapeHatchDescription`, `scanRingInstruction`, `scanWrongCode`, `scanTorchLabel`, `scanTorchUnavailable`, `scanCameraPermissionPrompt`, `scanOpenSettings`, `scanUnlockToScanTitle`, `scanUnlockToScanBody`. REUSE existing `dismissAlarmButton` for the escape Dismiss button — do NOT add a duplicate. `flutter gen-l10n` is a CI/human gate (toolchain absent) — the generated `AppLocalizations` getters do NOT exist on disk until codegen runs; note this so the executor does not expect `scan_task.dart` to compile locally.
  </action>
  <verify>
    <automated>grep -q 'AlarmTaskType.scan' lib/alarm/data/alarm_task_schemas.dart && grep -q '"scanTask"' lib/l10n/app_en.arb && grep -q '"scanEscapeHatch"' lib/l10n/app_en.arb && grep -q '"scanUnlockToScanTitle"' lib/l10n/app_en.arb && grep -q 'isVisual: false' lib/alarm/data/alarm_task_schemas.dart && { python3 -c "import json; json.load(open('lib/l10n/app_en.arb'))" || node -e "JSON.parse(require('fs').readFileSync('lib/l10n/app_en.arb','utf8'))"; } && echo PASS</automated>
  </verify>
  <acceptance_criteria>
    - `AlarmTaskType` enum ends with `scan` and no `alarmSchemaVersion` bump was made
    - `alarm_task_schemas.dart` has an `AlarmTaskType.scan:` entry with `StringSetting("Registered Code", ..., "", isVisual: false)` and `SwitchSetting("Escape Hatch", ..., true)` (default ON) and a builder returning `ScanTask(...)`
    - `app_en.arb` contains all 17 listed keys (incl. `scanUnlockToScanTitle`/`scanUnlockToScanBody`), each with a paired `"@key": {}` block; `dismissAlarmButton` is NOT duplicated (`grep -c '"dismissAlarmButton"' lib/l10n/app_en.arb` returns 1)
    - The ARB JSON is valid — the gate prefers `python3 -c "import json; json.load(...)"` and falls back to `node -e "JSON.parse(...)"` (portable: neither is guaranteed locally, but one is present in most environments; CI is the authoritative gate)
    - SUMMARY notes `flutter gen-l10n` is owed (the `AppLocalizations.scan*` getters are not on disk until CI/codegen)
  </acceptance_criteria>
  <done>scan enum value + schema (hidden code + escape toggle default ON + ScanTask builder) registered; all 17 ARB keys added with valid JSON (python3-preferred gate); dismissAlarmButton reused; gen-l10n owed.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Author the ScanTask ring widget (match→dismiss, escape, torch, camera release, camera-failure→unlock-to-scan)</name>
  <files>lib/alarm/widgets/tasks/scan_task.dart</files>
  <read_first>
    - lib/alarm/widgets/tasks/math_task.dart (the StatefulWidget(onSolve, settings) contract: `initialize()` reads settings via string keys, `initState`/`didUpdateWidget` call it, `dispose()` releases owned resources, `build()` is `Padding(all:16)` → `Column` with a `headlineMedium` header + theme-only colors via `Theme.of(context)`)
    - lib/alarm/logic/code_match.dart (Plan 02 — `normalizeCode`/`codesMatch`; apply normalize to BOTH the stored value and each scanned `code.text`)
    - lib/alarm/logic/escape_hatch_controller.dart (Plan 02 — `start()`/`recordFailedAttempt()`/`fireNow()`/`dispose()`; onEscapeAvailable callback flips a flag that reveals the Dismiss button)
    - lib/notifications/logic/alarm_notifications.dart (lines 105/182 — `FlutterShowWhenLocked().show()`/`.hide()` already open the over-lock window for EVERY ring; the scanner inherits it. NOTE: the `flutter_show_when_locked` plugin exposes ONLY `show()`/`hide()` — there is NO keyguard-locked query in it; do NOT invent one. The reliable runtime no-go signal is therefore the camera-preview failure below, NOT a keyguard API.)
    - .planning/phases/04-qr-barcode-scan-to-dismiss-task/04-RESEARCH.md "Pattern 3: ReaderWidget result handling" (the exact `ReaderWidget` params: `codeFormat`, `showFlashlight: true`, `showToggleCamera: false`, `showGallery: false`, `scanDelay`, `onControllerCreated(controller, exception)`, `onScan(Code)`, `onScanFailure(Code)`) + "Symbology set" (the Format bitmask) + Pitfall 1 (dispose) + Pitfall 2 (debounce) + "The Lock-Screen Camera Spike" / decision matrix (black-preview/no-decode = the no-go condition Surface-4 handles)
    - .planning/phases/04-qr-barcode-scan-to-dismiss-task/04-UI-SPEC.md "Surface 3 — Ring-time scanner widget" (layout, wrong-scan ~600ms error flash + vibration, torch default OFF + scanTorchUnavailable, escape Dismiss reuses dismissAlarmButton + Semantics, camera release on every exit) + "Surface 4 — 'Unlock to scan' keyguard prompt" (centered block, scanUnlockToScanTitle/Body, alarm keeps ringing, escape hatch always underneath)
    - .planning/phases/04-qr-barcode-scan-to-dismiss-task/04-PATTERNS.md "scan_task.dart" section
    - .planning/phases/04-qr-barcode-scan-to-dismiss-task/04-03-PLAN.md (the lock-screen spike — its verdict updates the DOCUMENTED EXPECTED DEFAULT, recorded in 04-LOCKSCREEN-SPIKE.md, and is verified behaviorally in Plan 06; it is NOT consumed as a runtime per-OEM flag here)
  </read_first>
  <behavior>
    - On a scanned code where codesMatch(normalizeCode(code.text), storedNormalized) → onSolve() called exactly once (match → dismiss/advance)
    - On a non-matching valid decode → vibration haptic + a transient error-role visual flash (scanWrongCode) + escapeHatch.recordFailedAttempt(); onSolve NOT called
    - onControllerCreated(_, exception != null) → escapeHatch.fireNow() (camera-unavailable, SCAN-07) AND a camera-failure flag is set so build() renders the Surface-4 unlock-to-scan prompt instead of a dead/black scanner (D-LOCK-NOGO-UX)
    - When the camera-failure flag is set, build() renders the Surface-4 prompt (scanUnlockToScanTitle / scanUnlockToScanBody) — the scanner is NOT shown; the alarm keeps ringing; the escape hatch is still running (its time/attempt threshold still applies — fireNow already armed the Dismiss), so the user is never trapped even if the device never unlocks
    - When the escape hatch fires (time/attempts/fireNow) → a Semantics-wrapped Dismiss button (reusing dismissAlarmButton) appears; tapping it calls onSolve() (D-ESC-SCOPE: skips only this task). The Dismiss affordance is reachable in BOTH the normal-scanner state and the Surface-4 unlock-to-scan state.
    - Snooze: NO snooze affordance anywhere in this widget (SCAN-05 / D-RING-SNOOZE — snooze stays the pre-task SlideNotificationAction swipe, reachable without entering the scanner)
    - dispose(): the EscapeHatchController is disposed and the ReaderWidget leaves the tree so its CameraController is released (SCAN-11)
  </behavior>
  <action>
    Create `lib/alarm/widgets/tasks/scan_task.dart` as `class ScanTask extends StatefulWidget` with `required this.onSolve` (VoidCallback) + `required this.settings` (SettingGroup), mirroring `math_task.dart`'s shell. In `initialize()` read `storedNormalized = normalizeCode(widget.settings.getSetting("Registered Code").value)` and `escapeEnabled = widget.settings.getSetting("Escape Hatch").value`. Construct an `EscapeHatchController(onEscapeAvailable: () => setState(() => _escapeAvailable = true), enabled: escapeEnabled)` and `start()` it. Hold a `bool _cameraFailed = false;` flag.
    `build()`: `Padding(all:16)` → `Column` with a `headlineMedium` header (`scanRingInstruction`). Then branch on `_cameraFailed`:
      - `_cameraFailed == false` → render the live `ReaderWidget` filling the slot with the broad symbology `codeFormat` bitmask (RESEARCH symbology set: QR + DataMatrix + EAN-8/13 + UPC-A/E + Code128 + Code39 + ITF — narrow only if spurious reads surface, SCAN-04), `showFlashlight: true` (SCAN-09 torch, default OFF; on torch failure show `scanTorchUnavailable` and keep scanning — graceful no-flash), `showToggleCamera: false`, `showGallery: false`.
      - `_cameraFailed == true` → render the Surface-4 "unlock to scan" prompt: a centered block (24px breathing room, theme `surface`/`background`) with `scanUnlockToScanTitle` (`displaySmall`/`headlineMedium`) + `scanUnlockToScanBody` (`bodyMedium`). Do NOT mount the `ReaderWidget` in this branch (a black/dead preview is exactly what we are degrading away from). The alarm keeps ringing (no audio change here — the ring audio is the firing isolate's; this widget only changes the dismiss-step UI). The escape hatch keeps running underneath, so the Dismiss button (below) still appears on threshold/fireNow.
    `onScan`: if `codesMatch(normalizeCode(code.text), storedNormalized)` → `widget.onSolve()`; else `vibrate()` + transient `error`-role flash (~600ms, tune so it never strobes) showing `scanWrongCode` + `escapeHatch.recordFailedAttempt()` (D-RING-WRONGSCAN; debounced — `ReaderWidget.scanDelay` 1000ms rate-limits, count only distinct non-matching valid decodes, Pitfall 2).
    `onControllerCreated(_, exception)`: if `exception != null` → `escapeHatch.fireNow()` (SCAN-07) AND `setState(() => _cameraFailed = true)` so the Surface-4 prompt replaces the dead scanner (D-LOCK-NOGO-UX). This camera-preview-failure path is the RUNTIME DEVICE-STATE signal for "no-go" — it triggers on ANY device whose preview fails to start (the secure-keyguard-blanks-camera case the Plan 04-03 spike measured is one cause; a missing/busy camera is another). It is deliberately NOT a per-OEM lookup: you cannot enumerate all OEMs, so the verdict from the Plan 04-03 spike is recorded only as the EXPECTED DEFAULT per device class (go = preview renders over keyguard; no-go = unlock-to-scan is the norm on those OEMs) in 04-LOCKSCREEN-SPIKE.md and verified behaviorally in Plan 06 — the spike verdict is never wired into a runtime branch. (If a future revision adds a reliable secure-keyguard-locked native query, it could PRE-empt mounting the camera; for this phase the camera-failure signal is the confirmed-available mechanism and is sufficient — SCAN-07 already requires firing the escape on camera-unavailable.)
    When `_escapeAvailable`, render a `primary`-prominent Dismiss button (REUSE `dismissAlarmButton`) wrapped in `Semantics` (screen-reader reachable — ethics-critical) whose `onTap` calls `widget.onSolve()` (D-ESC-SCOPE skips only this task). The Dismiss button renders in BOTH branches (normal scanner AND the unlock-to-scan prompt) so the escape hatch is always underneath. Use `vibration` (already a dep) for the haptic. Theme-only colors/sizes via `Theme.of(context)` (`error` for wrong-scan, `primary` for torch-ON/Dismiss) — NEVER hardcode hex (UI-SPEC Color).
    `dispose()`: call `escapeHatch.dispose()`; the `ReaderWidget` releases its `CameraController` when removed from the tree (SCAN-11) — every exit path (match→onSolve, escape→onSolve, app→background, AND switching into the `_cameraFailed` branch which un-mounts the ReaderWidget) must leave the tree. Defensively, if you hold a controller ref via `onControllerCreated`, stop it on escape-fire. NEVER `logger`/`print` `code.text` (privacy / D-REG-DISPLAY). Do NOT add a snooze control (SCAN-05). Do NOT edit `alarm_notification_screen.dart` — `_setNextWidget` auto-picks-up the new task type (D-RING-LAYOUT: the scan task is just another full-screen task widget in the dismiss step, ZERO new ring orchestration).
    The widget references generated `AppLocalizations.scan*` getters that only exist after `flutter gen-l10n` (CI/human gate) — note it won't compile locally without codegen; CI is the authoritative compile gate.
  </action>
  <verify>
    <automated>grep -q 'class ScanTask' lib/alarm/widgets/tasks/scan_task.dart && grep -q 'codesMatch(normalizeCode' lib/alarm/widgets/tasks/scan_task.dart && grep -q 'fireNow' lib/alarm/widgets/tasks/scan_task.dart && grep -q 'dismissAlarmButton' lib/alarm/widgets/tasks/scan_task.dart && grep -q 'scanUnlockToScanTitle' lib/alarm/widgets/tasks/scan_task.dart && grep -q 'onControllerCreated' lib/alarm/widgets/tasks/scan_task.dart && echo PASS  # full compile/analyze is a CI gate (gen-l10n needed); toolchain absent locally</automated>
  </verify>
  <acceptance_criteria>
    - `scan_task.dart` defines `class ScanTask extends StatefulWidget` with `onSolve` + `settings`, mirroring math_task's contract
    - `onScan` calls `widget.onSolve()` ONLY when `codesMatch(normalizeCode(code.text), storedNormalized)` is true; otherwise records a failed attempt + haptic + transient `scanWrongCode` flash
    - `onControllerCreated` fires `escapeHatch.fireNow()` AND sets a camera-failure flag when `exception != null` (SCAN-07) — both behaviors present (`grep -c 'fireNow' lib/alarm/widgets/tasks/scan_task.dart` >= 1 AND a camera-failure flag is set in the same path)
    - When the camera-failure flag is set, build() renders the Surface-4 prompt referencing both `scanUnlockToScanTitle` and `scanUnlockToScanBody` (`grep -c 'scanUnlockToScanTitle' lib/alarm/widgets/tasks/scan_task.dart` >= 1 AND `grep -c 'scanUnlockToScanBody' lib/alarm/widgets/tasks/scan_task.dart` >= 1) and does NOT mount the `ReaderWidget` in that branch
    - The escape Dismiss affordance REUSES `dismissAlarmButton`, is wrapped in `Semantics`, calls `onSolve()` (D-ESC-SCOPE), and is reachable in BOTH the scanner and the unlock-to-scan states (escape hatch always underneath)
    - The camera-failure→Surface-4 decision is a RUNTIME device-state branch (driven by `onControllerCreated`), NOT a per-OEM lookup; no per-OEM table or spike-verdict constant is referenced in `scan_task.dart` (`grep -ci 'samsung\|xiaomi\|miui\|oneui\|pixel\|oem' lib/alarm/widgets/tasks/scan_task.dart` returns 0)
    - `ReaderWidget(showFlashlight: true)` (SCAN-09); `showToggleCamera: false`, `showGallery: false`
    - No snooze affordance exists in the widget (`grep -ci snooze lib/alarm/widgets/tasks/scan_task.dart` returns 0) (SCAN-05)
    - No `logger`/`print` of `code.text` (`grep -E "(logger|print).*code\.text" lib/alarm/widgets/tasks/scan_task.dart` returns nothing)
    - `dispose()` disposes the EscapeHatchController; widget leaves the tree on every exit (SCAN-11)
    - Full `flutter analyze`/compile is owed via CI (gen-l10n needed) — not claimed locally
  </acceptance_criteria>
  <done>ScanTask renders the live ReaderWidget, dismisses only on a normalized match, fires the escape on camera-unavailable, degrades a failed/black preview to the Surface-4 unlock-to-scan prompt (escape hatch still underneath), exposes a Semantics Dismiss + torch, never shows snooze, never logs the payload, and releases the camera on dispose; the spike verdict informs the documented default only, not a runtime branch; CI is the compile gate.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: AlarmTask(scan) JSON round-trip + schema-present CI test (SCAN-01)</name>
  <files>test/alarm/types/alarm_task_scan_test.dart</files>
  <read_first>
    - lib/alarm/types/alarm_task.dart (the `AlarmTask(type)` ctor, `toJson()` writing `id`/`schema`/`type.name`, and `AlarmTask.fromJson` resolving `AlarmTaskType.values.byName` + `alarmTaskSchemasMap[type]!.copy()`)
    - lib/alarm/data/alarm_task_schemas.dart (the scan schema added in Task 1 — `alarmTaskSchemasMap` must contain `AlarmTaskType.scan`)
    - test/alarm/types/alarm_snooze_test.dart (Phase-2 discipline: pure JSON/flag assertions; OS calls no-op under FLUTTER_TEST; header comment style)
    - .planning/phases/04-qr-barcode-scan-to-dismiss-task/04-RESEARCH.md "Phase Requirements → Test Map" (SCAN-01 row: AlarmTask(scan) round-trips through toJson/fromJson; schema present in map)
  </read_first>
  <behavior>
    - alarmTaskSchemasMap contains AlarmTaskType.scan (schema present)
    - AlarmTask(AlarmTaskType.scan) has settings with a "Registered Code" and an "Escape Hatch" setting; Escape Hatch defaults to true
    - Setting a registered code value, then AlarmTask.fromJson(task.toJson()) preserves type==scan and the stored code value + the Escape Hatch flag (round-trip)
    - A scan task with no registered code keeps "Registered Code" empty after round-trip (no crash; save-gate enforced in Plan 05, not here)
  </behavior>
  <action>
    Create `test/alarm/types/alarm_task_scan_test.dart` asserting: `alarmTaskSchemasMap.containsKey(AlarmTaskType.scan)` is true; an `AlarmTask(AlarmTaskType.scan)` exposes `getSetting("Registered Code")` and `getSetting("Escape Hatch")` (default `true`); after writing a code into "Registered Code", `AlarmTask.fromJson(task.toJson())` yields `type == AlarmTaskType.scan` with the same stored code and Escape-Hatch flag (round-trip — SCAN-01). Mirror `alarm_snooze_test.dart` discipline (assert on objects/JSON only; no camera/UI). This is the SCAN-01 regression gate. Toolchain absent — CI (`tests.yml`) is authoritative; do NOT claim local green.
  </action>
  <verify>
    <automated>flutter test test/alarm/types/alarm_task_scan_test.dart   # AUTHORITATIVE in CI (tests.yml); toolchain absent locally — confirm green via CI</automated>
  </verify>
  <acceptance_criteria>
    - The test asserts `alarmTaskSchemasMap.containsKey(AlarmTaskType.scan)`
    - The test round-trips `AlarmTask(scan)` through `toJson`/`fromJson` and asserts `type == AlarmTaskType.scan` plus a preserved registered-code value and Escape-Hatch flag
    - The test asserts the Escape-Hatch setting defaults to `true` on a fresh scan task (SCAN-06)
    - The test imports no camera/UI package (pure JSON/settings assertions)
    - Runs under `tests.yml` and passes — confirmed via CI, NOT claimed locally
  </acceptance_criteria>
  <done>alarm_task_scan_test.dart proves the scan schema is registered and AlarmTask(scan) round-trips with its code + escape flag; CI is the authoritative gate.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| camera preview ↔ main isolate ring screen | Camera lifecycle runs in the main-isolate notification screen, never the firing isolate (ARCHITECTURE constraint) |
| scanned payload → match gate | Untrusted decoded `code.text` crosses into the dismiss gate via codesMatch |
| escape controller → dismiss bypass | The escape hatch is the anti-trap accessibility control that gates the dismiss bypass |
| failed/black camera preview → unlock-to-scan degradation | A device whose preview fails (e.g. secure keyguard blanks the camera) must degrade to the unlock-to-scan prompt with the escape hatch underneath, not a dead scanner |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-09 | Information Disclosure | ScanTask camera not released | mitigate | ReaderWidget.dispose() releases the CameraController on every exit path; defensive controller-stop on escape-fire (SCAN-11; verified on-device in Plan 06) |
| T-04-10 | Information Disclosure | logging code.text | mitigate | No `logger`/`print` of the decoded payload in scan_task.dart (ASVS V7 / D-REG-DISPLAY) |
| T-04-11 | Denial of Service (against the user) | un-dismissable scan task | mitigate | Default-ON escape hatch + fireNow() on camera-unavailable surfaces a Semantics Dismiss button (SCAN-06/07); the Dismiss is reachable in the unlock-to-scan state too |
| T-04-12 | Tampering/Injection | normalizeCode(code.text) | mitigate | Opaque-string compare only; never executed/parsed into an action (ASVS V5) |
| T-04-21 | Denial of Service (against the user) | dead/black camera preview over a secure keyguard (no-go OEM) | mitigate | onControllerCreated failure → Surface-4 unlock-to-scan prompt instead of a dead scanner; the alarm keeps ringing and the escape hatch keeps running underneath (D-LOCK-NOGO-UX); behavioral confirm on a no-go device in Plan 06 |
</threat_model>

<verification>
- SCAN-01: `alarm_task_scan_test.dart` proves the schema is registered and AlarmTask(scan) round-trips (CI gate).
- SCAN-03: ScanTask gates onSolve behind `codesMatch(normalizeCode(...))` (the seam is CI-tested in Plan 02; the wiring is source-asserted here).
- SCAN-04: broad symbology bitmask set on the ReaderWidget.
- SCAN-05: no snooze affordance in the widget (grep gate).
- SCAN-06/07: escape default ON; fireNow on camera-unavailable; Semantics Dismiss; camera-failure also drives the Surface-4 degradation.
- D-LOCK-NOGO-UX: camera-preview failure (onControllerCreated exception / dead preview) → Surface-4 unlock-to-scan prompt (scanUnlockToScanTitle/Body) with the escape hatch underneath; this is a runtime device-state branch (NOT a per-OEM lookup); the Plan 04-03 spike verdict updates only the documented expected default, recorded for verification; live no-go behavior is confirmed in Plan 06's on-device checkpoint (which already lists "no-go OEM degradation").
- SCAN-09: ReaderWidget(showFlashlight: true) + graceful torch-failure copy.
- SCAN-11: dispose releases the camera (behavioral confirmation owed on-device in Plan 06).
- SCAN-12: all ring strings localized in app_en.arb.
- Real ring-time scan, torch, camera-release, and no-go unlock-to-scan behavior are on-device — verified in Plan 06 (not here).
</verification>

<success_criteria>
- A user can add a "Scan code to dismiss" task; it appears alongside the existing tasks.
- ScanTask dismisses only on a normalized match, fires the escape on camera-unavailable, degrades a failed/black preview to the Surface-4 unlock-to-scan prompt (escape hatch underneath), exposes a Semantics Dismiss + torch, shows no snooze, never logs the payload, and releases the camera on dispose.
- AlarmTask(scan) round-trips in a green CI test.
- alarm_notification_screen.dart is NOT edited (zero ring-orchestration change).
</success_criteria>

<output>
Create `.planning/phases/04-qr-barcode-scan-to-dismiss-task/04-04-SUMMARY.md` when done. Record the symbology set chosen, the camera-failure→Surface-4 device-state mechanism (and that the Plan 04-03 spike verdict informs only the documented expected default, not a runtime branch), that gen-l10n + flutter analyze/test are owed via CI, and that real ring-time scan/torch/camera-release/no-go-degradation are deferred to the Plan 06 on-device checkpoint.
</output>
</content>
