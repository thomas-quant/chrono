---
phase: 04-qr-barcode-scan-to-dismiss-task
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - pubspec.yaml
  - android/app/build.gradle
  - android/app/src/main/AndroidManifest.xml
  - .github/workflows/test-apk.yml
autonomous: true
requirements: [BUILD-01, BUILD-02, SCAN-08]
user_setup: []

must_haves:
  truths:
    - "The prod (F-Droid) release Gradle dependency graph contains zero mlkit/gms/play-services entries"
    - "minSdk is 23 so the flutter_zxing native build is satisfiable"
    - "CAMERA permission and uses-feature camera required=false are declared in the manifest"
    - "flutter_zxing is pinned to exactly 2.2.1 (not a caret range)"
  artifacts:
    - path: "pubspec.yaml"
      provides: "flutter_zxing 2.2.1 exact pin"
      contains: "flutter_zxing: 2.2.1"
    - path: "android/app/build.gradle"
      provides: "minSdk 23"
      contains: "minSdkVersion 23"
    - path: "android/app/src/main/AndroidManifest.xml"
      provides: "CAMERA permission + uses-feature required=false"
      contains: "android.permission.CAMERA"
    - path: ".github/workflows/test-apk.yml"
      provides: "BUILD-02 zero-ML-Kit CI gate"
      contains: "mlkit"
  key_links:
    - from: "pubspec.yaml flutter_zxing 2.2.1"
      to: "android/app/build.gradle minSdkVersion 23"
      via: "plugin android/build.gradle hard-codes minSdkVersion 23"
      pattern: "minSdkVersion 23"
    - from: ".github/workflows/test-apk.yml dependency-graph job"
      to: "prodReleaseRuntimeClasspath"
      via: "gradlew :app:dependencies grep mlkit|gms|play-services"
      pattern: "mlkit|play-services|gms"
---

<objective>
Land the build-enablement gate that every other scan-task plan depends on: pin `flutter_zxing: 2.2.1` (exact, not a caret — per D-CLEANROOM-OVERRIDE-independent BUILD-02), raise `minSdkVersion` 21 → 23 (BUILD-01), declare `CAMERA` + `uses-feature camera required="false"` in the manifest (SCAN-08), and add a CI job that proves the prod (F-Droid) release dependency graph contains zero `mlkit`/`gms`/`play-services` (BUILD-02 exit criterion).

Purpose: This is the foundation gate. The scanner native build cannot compile without the pin + minSdk bump, the spike cannot render a `ReaderWidget` over the keyguard without the CAMERA permission, and the F-Droid distribution constraint is only defensible if the zero-ML-Kit gate runs in CI. No Dart production code here — pure config + CI wiring. Implements BUILD-01, BUILD-02, SCAN-08.

Output: edited `pubspec.yaml`, `android/app/build.gradle`, `AndroidManifest.xml`, and a new dependency-graph verification job in `.github/workflows/test-apk.yml`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-qr-barcode-scan-to-dismiss-task/04-RESEARCH.md
@.planning/phases/04-qr-barcode-scan-to-dismiss-task/04-PATTERNS.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Pin flutter_zxing 2.2.1 and bump minSdk to 23</name>
  <files>pubspec.yaml, android/app/build.gradle</files>
  <read_first>
    - pubspec.yaml (the existing `dependencies:` block — see the git-fork pins for `android_alarm_manager_plus`, `flutter_foreground_task` for formatting precedent)
    - android/app/build.gradle (line 38 `ndkVersion flutter.ndkVersion`, line 56 `minSdkVersion 21`)
    - .planning/phases/04-qr-barcode-scan-to-dismiss-task/04-RESEARCH.md "Standard Stack" (why 2.2.1 exactly; 2.3.0 needs Flutter >=3.41) + Pitfall 4 (NDK 27.0.12077973 / CMake)
    - .planning/phases/04-qr-barcode-scan-to-dismiss-task/04-PATTERNS.md "Non-Dart edits (config — exact self-analogs)" (the pubspec/gradle lines)
  </read_first>
  <action>
    In `pubspec.yaml` add `flutter_zxing: 2.2.1` to the `dependencies:` block as an EXACT pin — NOT `^2.2.0`. A caret would let pub resolve into `2.3.0`, which requires Flutter >=3.41 and breaks Chrono's 3.22.2 (RESEARCH "Why 2.2.1 exactly"). Add an inline comment `# exact pin — NOT ^2.2.0 (2.3.0 needs Flutter >=3.41)`.
    In `android/app/build.gradle` change `minSdkVersion 21` (line 56) to `minSdkVersion 23` (BUILD-01 — the plugin's own android/build.gradle hard-codes minSdkVersion 23). Leave `ndkVersion flutter.ndkVersion` (line 38) AS-IS for now; only if the native build later complains in CI does it get aligned to `27.0.12077973` — note that contingency in the SUMMARY, do not change it preemptively. Do not touch compileSdk (stays 34) or the flavor config.
    Do NOT run `flutter pub get` — the toolchain is absent locally; the lockfile is generated/verified in CI (CLAUDE.md). Do not hand-edit `pubspec.lock`.
  </action>
  <verify>
    <automated>grep -q '^  flutter_zxing: 2.2.1' pubspec.yaml && grep -q 'minSdkVersion 23' android/app/build.gradle && ! grep -q 'flutter_zxing: \^' pubspec.yaml && echo PASS</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c 'flutter_zxing: 2.2.1' pubspec.yaml` returns 1 and `grep -c 'flutter_zxing: \^' pubspec.yaml` returns 0 (exact pin, no caret)
    - `grep -c 'minSdkVersion 23' android/app/build.gradle` returns 1 and `grep -c 'minSdkVersion 21' android/app/build.gradle` returns 0
    - `pubspec.lock` is NOT modified by hand (lockfile resolution is a CI gate; toolchain absent locally)
    - SUMMARY records the NDK-alignment contingency (set ndkVersion to 27.0.12077973 only if CI native build complains)
  </acceptance_criteria>
  <done>flutter_zxing is exactly 2.2.1, minSdk is 23, no caret pin, ndkVersion left as flutter.ndkVersion with the contingency noted.</done>
</task>

<task type="auto">
  <name>Task 2: Declare CAMERA permission and uses-feature camera required=false (SCAN-08)</name>
  <files>android/app/src/main/AndroidManifest.xml</files>
  <read_first>
    - android/app/src/main/AndroidManifest.xml (the existing `uses-permission` block, lines 4-22 — mirror its formatting/indentation)
    - .planning/phases/04-qr-barcode-scan-to-dismiss-task/04-RESEARCH.md "Manifest additions (SCAN-08)" (the exact element shape; Alarmy's manifest confirmed this shape, autofocus/flash/front all required=false)
    - .planning/phases/04-qr-barcode-scan-to-dismiss-task/04-PATTERNS.md "Non-Dart edits → AndroidManifest.xml"
  </read_first>
  <action>
    Add to `android/app/src/main/AndroidManifest.xml`, alongside the existing `uses-permission` block (preserve the file's existing indentation): `<uses-permission android:name="android.permission.CAMERA" />`, `<uses-feature android:name="android.hardware.camera" android:required="false" />`, `<uses-feature android:name="android.hardware.camera.autofocus" android:required="false" />`, and `<uses-feature android:name="android.hardware.camera.flash" android:required="false" />`. The `required="false"` on the camera feature is mandatory so the Play listing is not camera-gated (SCAN-08 verbatim) — devices without a camera can still install; the escape hatch covers them.
    This satisfies the manifest half of SCAN-08. The runtime permission REQUEST (at setup, never fire time) is built in Plan 05's registration card — do not add any runtime request here.
  </action>
  <verify>
    <automated>grep -q 'android.permission.CAMERA' android/app/src/main/AndroidManifest.xml && grep -q 'android.hardware.camera"' android/app/src/main/AndroidManifest.xml && grep -q 'android:required="false"' android/app/src/main/AndroidManifest.xml && echo PASS</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c 'android.permission.CAMERA' android/app/src/main/AndroidManifest.xml` returns 1
    - The line `<uses-feature android:name="android.hardware.camera" android:required="false" />` is present (camera feature is NOT required — Play listing not camera-gated)
    - `android.hardware.camera.autofocus` and `android.hardware.camera.flash` uses-feature lines are present, both `required="false"`
    - No runtime permission-request code was added to any Kotlin/Dart file in this task (request lives at setup in Plan 05)
  </acceptance_criteria>
  <done>CAMERA permission + camera/autofocus/flash uses-feature (all required=false) declared in the manifest; no runtime request added here.</done>
</task>

<task type="auto">
  <name>Task 3: Add the BUILD-02 zero-ML-Kit dependency-graph CI gate</name>
  <files>.github/workflows/test-apk.yml</files>
  <read_first>
    - .github/workflows/test-apk.yml (the existing job structure — Java 17 setup, Flutter 3.22.2 pin, `flutter analyze`, dev APK build steps; mirror the runner/setup block)
    - .planning/phases/04-qr-barcode-scan-to-dismiss-task/04-RESEARCH.md "BUILD-02 zero-ML-Kit gate (CI command, NOT local)" (the exact gradlew command + prodReleaseRuntimeClasspath configuration) + Pitfall 3 (the camera/image_picker transitive risk this gate catches)
    - CLAUDE.md Testing Policy (CI is the authoritative gate; toolchain absent locally)
  </read_first>
  <action>
    Add a CI step (or a new job sharing the same Flutter 3.22.2 + Java 17 setup as the existing `test-apk.yml` jobs) that, after `flutter pub get`, runs `cd android && ./gradlew :app:dependencies --configuration prodReleaseRuntimeClasspath` and pipes the output through `grep -Ei 'mlkit|play-services|gms'`. The step MUST FAIL the build if any match is found (the grep matching is the FAIL condition) and PASS (exit 0) when there are zero matches. Target the `prod` (F-Droid) flavor's release runtime classpath specifically — that is the artifact F-Droid ships. Keep this as a blocking step on the scan-feature branch (do not mark `continue-on-error: true` — BUILD-02 is a hard exit criterion, unlike the informational `flutter analyze` step which keeps `continue-on-error`).
    Use a small shell snippet so a grep MATCH yields a non-zero exit (e.g. `if ... | grep -Eiq 'mlkit|play-services|gms'; then echo "FAIL: non-FOSS dep present"; exit 1; else echo "PASS: zero ML Kit"; fi`). Do NOT add an emulator / integration_test job — RESEARCH explicitly recommends against it (a secure-keyguard + real camera condition is not reproducible in an emulator job).
    Per CLAUDE.md / STATE.md: this gate is OWED via CI on push — do NOT claim it passed locally (no Android SDK / NDK / network here). Frame CI as the authoritative gate in the SUMMARY.
  </action>
  <verify>
    <automated>grep -Eq 'mlkit|play-services|gms' .github/workflows/test-apk.yml && grep -q 'prodReleaseRuntimeClasspath' .github/workflows/test-apk.yml && grep -q 'app:dependencies' .github/workflows/test-apk.yml && echo PASS</automated>
  </verify>
  <acceptance_criteria>
    - `.github/workflows/test-apk.yml` contains a step running `./gradlew :app:dependencies --configuration prodReleaseRuntimeClasspath`
    - That step greps for `mlkit|play-services|gms` and `exit 1`s on a match (a non-FOSS dep FAILS the build), passing only on zero matches
    - The dependency-graph step is NOT `continue-on-error: true` (BUILD-02 is a hard gate)
    - No emulator / `reactivecircus/android-emulator-runner` / integration_test job was added (RESEARCH recommendation)
    - SUMMARY states the gate is authoritative in CI and was NOT run locally (toolchain absent)
  </acceptance_criteria>
  <done>CI gate greps the prod release classpath for mlkit/gms/play-services and fails on any match; owed-via-CI noted; no emulator job added.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| pub.dev → app build | A pinned third-party plugin (`flutter_zxing`) and its transitive deps cross into the build; a non-FOSS transitive dep would break F-Droid provenance |
| package manager install → Gradle graph | `flutter pub get` resolves transitive Android artifacts (`camera`, `image_picker`) that could in theory pull Play Services |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-01 | Tampering (supply-chain) | flutter_zxing transitive `camera`/`image_picker` | mitigate | BUILD-02 CI gate greps prodReleaseRuntimeClasspath for mlkit/gms/play-services; fails build on any match (Task 3) |
| T-04-02 | Tampering (version drift) | pubspec flutter_zxing pin | mitigate | Exact pin `2.2.1` (no caret) prevents auto-upgrade into Flutter-3.41-only 2.3.0 (Task 1) |
| T-04-SC | Tampering | pub install (flutter_zxing) | mitigate | RESEARCH Package Legitimacy Audit approved flutter_zxing via pub.dev API (147 likes, 49K downloads/30d, real GitHub source). The lone slopcheck [SLOP] is a documented PyPI-vs-pub ecosystem-mismatch false positive — disregarded per RESEARCH. No [ASSUMED]/[SUS]/[SLOP] package remains; no blocking human checkpoint required. |
</threat_model>

<verification>
- BUILD-01: `grep minSdkVersion android/app/build.gradle` shows 23, not 21.
- BUILD-02: `.github/workflows/test-apk.yml` runs the prod-release dependency-graph grep gate; exact pin present.
- SCAN-08 (manifest half): CAMERA + uses-feature camera required=false present.
- All three are config/CI changes; the authoritative behavioral confirmation (native build compiles + zero ML Kit) is OWED via the CI job on push — toolchain absent locally.
</verification>

<success_criteria>
- `flutter_zxing: 2.2.1` exact pin in pubspec.yaml (no caret).
- `minSdkVersion 23` in android/app/build.gradle.
- CAMERA + uses-feature camera required=false in AndroidManifest.xml.
- A blocking CI step greps the prod release Gradle graph for mlkit/gms/play-services and fails on any match.
- No Dart production code changed; no emulator job added.
</success_criteria>

<output>
Create `.planning/phases/04-qr-barcode-scan-to-dismiss-task/04-01-SUMMARY.md` when done. Record: the exact pin rationale, the NDK-alignment contingency (unused unless CI complains), and that BUILD-02 / native-build verification is owed via CI (not run locally).
</output>
