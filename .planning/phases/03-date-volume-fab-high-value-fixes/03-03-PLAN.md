---
phase: 03-date-volume-fab-high-value-fixes
plan: 03
type: execute
wave: 1
depends_on: []
files_modified:
  - lib/common/widgets/list/custom_list_view.dart
  - test/common/widgets/list/fab_clearance_test.dart
autonomous: true
requirements: [FAB-01, PR-02]
must_haves:
  truths:
    - "Floating action buttons no longer cover the last list item or menu buttons on the alarm and other list screens"
    - "Every screen rendering its list through CustomListView inherits the bottom clearance from one central edit"
    - "The reserved bottom inset clears the FAB extent including the Material-style +20 offset"
  artifacts:
    - path: "lib/common/widgets/list/custom_list_view.dart"
      provides: "A computed bottom inset on the AnimatedReorderableListView padding that reserves clearance for the FAB"
      contains: "EdgeInsets.only"
    - path: "test/common/widgets/list/fab_clearance_test.dart"
      provides: "Narrow headless widget test asserting the list reserves bottom clearance >= FAB extent"
  key_links:
    - from: "lib/common/widgets/list/custom_list_view.dart (padding)"
      to: "AnimatedReorderableListView -> underlying scrollable"
      via: "padding forwarded to padding ?? EdgeInsets.zero in animated_reorderable_listview.dart:250"
      pattern: "padding:\\s*EdgeInsets"
    - from: "lib/common/widgets/list/custom_list_view.dart (bottom inset)"
      to: "theme.extension<ThemeSettingExtension>().useMaterialStyle"
      via: "the +20 Material offset matches fab.dart:67-69 so the inset clears the FAB"
      pattern: "useMaterialStyle"
---

<objective>
Fix the FAB-over-list overlap (FAB-01) at the single shared list/FAB layer (D-FAB-SCOPE), reimplemented
independently (PR-02, sole credit per D-PR-METHOD — no contributor attribution). Inject one computed
bottom inset into `CustomListView`'s `AnimatedReorderableListView.padding` — the single point every one
of the ~13 FAB screens renders its list through — so the last item and menu buttons are never hidden,
across all list screens in one edit. The FAB itself is a sibling `Positioned` overlay and does not move;
only the scroll content gains bottom room. Derive the clearance constant from `fab.dart` and the
existing `getSnackbar` precedent (FAB extent + gap + Material `+20`), not by guessing. Author a narrow
headless widget test asserting the list reserves clearance ≥ the FAB extent.

Purpose: The FAB is a custom `Positioned` widget stacked over the list (not a Material
`Scaffold.floatingActionButton`), so the scrollable does not auto-reserve space — hence the last item /
bottom alarm's menu button is occluded (#417, #463). One central bottom-inset edit is the minimal
correct fix and covers every list screen.

Output: A one-edit bottom inset in `lib/common/widgets/list/custom_list_view.dart` and
`test/common/widgets/list/fab_clearance_test.dart`, confirmed green via CI.

Note (D-PR-METHOD / deferred): this reimplements PR #466 independently with NO contributor attribution
(sole credit). PR-02 and ROADMAP success-criterion #4 still literally say "crediting the contributor" —
reword at the next `/gsd-transition`. Do NOT reword here; do NOT add any contributor attribution.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-date-volume-fab-high-value-fixes/03-CONTEXT.md
@.planning/phases/03-date-volume-fab-high-value-fixes/03-RESEARCH.md
@.planning/phases/03-date-volume-fab-high-value-fixes/03-PATTERNS.md
@.planning/codebase/CONVENTIONS.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Inject one computed bottom inset into CustomListView's list padding</name>
  <files>lib/common/widgets/list/custom_list_view.dart</files>
  <read_first>
    - lib/common/widgets/list/custom_list_view.dart (read 1-60 for imports/class; read 360-408 — the `AnimatedReorderableListView` with the hardcoded `padding: const EdgeInsets.symmetric(horizontal: 16, vertical: 8)` at :390-391 — the single injection point; confirm where `theme`/`Theme.of(context)` is available in `build`)
    - lib/common/widgets/fab.dart (FULL — the FAB's own bottom offset `bottomPadding + (useMaterialStyle ? 20 : 0)` at :67-69; tap target = 16 pad + 24 icon + 16 pad ≈ 56 from :84,88; reads `theme.extension<ThemeSettingExtension>()` at :58-59)
    - lib/common/utils/snackbar.dart (FULL — the clearance-math precedent in `getSnackbar` at :51-69: FAB extent `64 + 16`, Material `+20`, navBar adjustment)
    - lib/common/widgets/list/persistent_list_view.dart (confirm at :179 it delegates to CustomListView and passes NO padding — so the fix belongs HERE, not in the wrapper)
    - lib/navigation/screens/nav_scaffold.dart (read ~225-255 — in landscape there is no `bottomNavigationBar` at :230 and `SafeArea` already wraps the body at :252; the inset must clear the FAB but needn't add nav-bar height)
    - .planning/phases/03-date-volume-fab-high-value-fixes/03-PATTERNS.md (custom_list_view section lines 131-178 — injection point, clearance math, theme-extension access, derivation guidance; "DO NOT guess the constant; derive it")
    - .planning/phases/03-date-volume-fab-high-value-fixes/03-RESEARCH.md (Pattern 3 lines 245-258; Pitfall 4 lines 312-315)
  </read_first>
  <action>
    In `CustomListView.build`, read `ThemeSettingExtension themeSettings =
    theme.extension<ThemeSettingExtension>()!;` (mirror `fab.dart:58-59`; add
    `import 'package:clock_app/theme/types/theme_extension.dart';`). Replace the hardcoded
    `padding: const EdgeInsets.symmetric(horizontal: 16, vertical: 8)` at custom_list_view.dart:390-391
    with `padding: EdgeInsets.only(left: 16, right: 16, top: 8, bottom: <computed>)` where `<computed>`
    = the existing vertical `8` + the FAB tap-target extent (≈56, derived from fab.dart's
    16+24+16 padding/icon) + a gap (16, matching the getSnackbar `+16` and fab.dart's `16` right offset)
    + the Material offset `(themeSettings.useMaterialStyle ? 20 : 0)` (matching fab.dart:67-69 so the
    inset clears the FAB's own bottom offset). Derive each term from `fab.dart` + `getSnackbar` — do NOT
    pick an arbitrary number (Pitfall 4). Do not add nav-bar height (SafeArea already wraps the body and
    landscape has no bottom nav bar). Make this the single edit — do NOT add a padding parameter to
    `PersistentListView` (it is a pass-through wrapper; the central edit covers all ~13 screens). Keep
    the horizontal `16` and top `8` exactly as before so only the bottom inset changes. Do NOT add any
    contributor attribution or PR #466 reference.
    NOTE for the executor: cite the derivation of each constant (which fab.dart / snackbar.dart line it
    came from) in a brief code comment and in the SUMMARY, so the value is auditable rather than magic.
  </action>
  <acceptance_criteria>
    - custom_list_view.dart's `AnimatedReorderableListView` uses `EdgeInsets.only(...)` with a bottom inset that includes the FAB extent + gap + `(useMaterialStyle ? 20 : 0)`; horizontal stays 16 and top stays 8
    - `CustomListView.build` reads `theme.extension<ThemeSettingExtension>()` and the file imports `theme_extension.dart`
    - The bottom inset value is derived (a comment cites the fab.dart / snackbar.dart source of each term) — not an undocumented literal
    - `persistent_list_view.dart` is NOT modified (the central edit is sufficient)
    - No contributor attribution / PR #466 reference is added (grep `466`, contributor names → none)
    - Verified green via CI: `flutter test` green in `tests.yml`; `flutter analyze` (informational, test-apk.yml) shows no NEW issues on `lib/common/widgets/list/custom_list_view.dart`
  </acceptance_criteria>
  <verify>
    <automated>Source-level: grep `lib/common/widgets/list/custom_list_view.dart` for `EdgeInsets.only`, `useMaterialStyle`, and the `theme_extension` import; confirm horizontal 16 / top 8 preserved. Behavioral coverage via Task 2 (CI widget test).</automated>
  </verify>
  <done>One central bottom inset on the shared list scrollable reserves clearance for the FAB across all list screens; derived (not guessed); PersistentListView untouched; no PR attribution.</done>
</task>

<task type="auto">
  <name>Task 2: Narrow headless widget test — list reserves bottom clearance >= FAB extent</name>
  <files>test/common/widgets/list/fab_clearance_test.dart</files>
  <read_first>
    - test/common/widgets/fields/date_picker_field_test.dart and test/common/widgets/fields/switch_field_test.dart (the headless `pumpWidget` analog: narrowly-scoped widget, assert on layout/finders, no full app shell)
    - lib/common/widgets/list/custom_list_view.dart (the edited build — how the list is constructed, what minimal props `CustomListView`/`AnimatedReorderableListView` need, and that it reads `theme.extension<ThemeSettingExtension>()`)
    - lib/common/widgets/fab.dart (the FAB extent the test asserts the inset clears: ≈56 + Material +20)
    - lib/theme/types/theme_extension.dart (the `ThemeSettingExtension` the pumped theme must carry, since the seam reads it)
    - test/alarm/types/alarm_snooze_test.dart (the `TestWidgetsFlutterBinding.ensureInitialized()` + appSettings-reachability discipline)
    - .planning/phases/03-date-volume-fab-high-value-fixes/03-PATTERNS.md (fab_clearance_test section lines 227-231 — keep narrow to avoid appSettings/storage/l10n brittleness; degrade to on-device-only if too flaky, documented)
    - .planning/phases/03-date-volume-fab-high-value-fixes/03-RESEARCH.md (Wave 0 gap line 448 — narrow seam, theme extension carried)
  </read_first>
  <action>
    Create `test/common/widgets/list/fab_clearance_test.dart` as a narrow headless widget test
    (`testWidgets`, `pumpWidget`), mirroring the `test/common/widgets/fields/*_test.dart` scope. Pump the
    smallest widget tree that exercises the seam: a `MaterialApp`/`Theme` whose `ThemeData` carries a
    `ThemeSettingExtension` (so `theme.extension<ThemeSettingExtension>()!` resolves — copy how an
    existing theme-extension widget test or the app theme sets it up; supply a known `useMaterialStyle`),
    wrapping the list under test. Assert the resolved bottom padding on the scrollable's `EdgeInsets`
    (find the relevant scroll/list widget via a `Finder` and read its `padding`) reserves a bottom inset
    ≥ the FAB extent (the ≈56 + the Material `+20` when `useMaterialStyle` is true). Prefer asserting the
    numeric bottom inset directly (the clearance contract) over pixel geometry, to stay robust. Keep the
    test MINIMAL — avoid pulling in the full `appSettings`/storage/l10n singletons that make full-screen
    widget tests flaky; if construction unavoidably needs `appSettings`, initialize only the minimum (or
    use `TestWidgetsFlutterBinding.ensureInitialized()` + a fake/minimal list controller). If the seam
    cannot be exercised headlessly without heavy singletons and proves flaky, degrade this to an
    on-device-only check and document the degradation in the SUMMARY (D-TEST-COVERAGE explicitly permits
    this) — but attempt the narrow headless test first. Do NOT touch `just_audio`/storage. Do NOT add a
    pub dependency.
  </action>
  <acceptance_criteria>
    - File `test/common/widgets/list/fab_clearance_test.dart` exists with a `testWidgets(...)` that `pumpWidget`s a narrowly-scoped tree carrying a `ThemeSettingExtension`
    - The test asserts the list/scrollable's bottom inset is ≥ the FAB extent (a numeric clearance assertion referencing the FAB extent + Material +20)
    - The test is narrow (does not boot the full nav scaffold / does not require real storage files); grep confirms no full `App()`/`NavScaffold` construction
    - No new pub dependency is added (pubspec.yaml unchanged)
    - Verified green via CI: `flutter test test/common/widgets/list/fab_clearance_test.dart` is green in `tests.yml` on push (toolchain absent locally — CI is the authoritative gate). If it proves flaky/unworkable headlessly, it is degraded to on-device-only and that degradation is documented in the SUMMARY.
  </acceptance_criteria>
  <verify>
    <automated>flutter test test/common/widgets/list/fab_clearance_test.dart</automated>
  </verify>
  <done>A narrow headless widget test asserts the list reserves bottom clearance >= the FAB extent (incl. Material +20); it adds no dependency and is confirmed green via CI, or is degraded to on-device-only with documentation if headless proves too brittle.</done>
</task>

</tasks>

<verification>
- `flutter test test/common/widgets/list/fab_clearance_test.dart` green in CI (`tests.yml`) — or documented on-device-only degradation.
- `flutter analyze` (informational, via `test-apk.yml` dispatch) shows no NEW issues on `lib/common/widgets/list/custom_list_view.dart`, `test/common/widgets/list/fab_clearance_test.dart`.
- Source: one `EdgeInsets.only` bottom inset in `custom_list_view.dart` derived from fab.dart/snackbar.dart; `persistent_list_view.dart` untouched.
- No contributor attribution / PR #466 reference in changed files.
- On-device (human gate, CI cannot run): real cross-OEM pixel layout — the last list item and the bottommost alarm's menu button are fully visible above the FAB in portrait and landscape, Material and non-Material styles.
</verification>

<success_criteria>
- FAB-01: floating action buttons no longer cover list items or menu buttons on the alarm and other list screens — fixed once centrally, inherited by all ~13 FAB screens.
- PR-02 (reframed per D-PR-METHOD): the #466 FAB fix is reimplemented independently, sole credit, no contributor attribution; the clearance fully clears FAB-01's no-overlap (D-PR-QUALITY).
</success_criteria>

<output>
Create `.planning/phases/03-date-volume-fab-high-value-fixes/03-03-SUMMARY.md` when done. Record: the exact derived bottom-inset constant and which fab.dart/snackbar.dart lines each term came from; whether the headless FAB test stayed in CI or degraded to on-device-only (and why); that PR-02/criterion #4 still need rewording at the next transition (deferred, not done here); that `flutter test`/`flutter analyze` are owed via CI; the on-device cross-OEM layout check as a remaining human gate.
</output>
