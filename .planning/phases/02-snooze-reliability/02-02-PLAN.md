---
phase: 02-snooze-reliability
plan: 02
type: execute
wave: 2
depends_on: ["02-01"]
files_modified:
  - test/alarm/types/alarm_snooze_test.dart
  - .github/workflows/test-apk.yml
autonomous: true
requirements: [SNZ-01, SNZ-02, SNZ-03, SNZ-04, SNZ-05]

must_haves:
  truths:
    - "A CI-runnable test suite asserts every SNZ-01..05 behavior on Alarm flags without a device"
    - "SNZ-02: a 0.5-min snooze pins _snoozeTime to exactly now+30s under a frozen clock"
    - "SNZ-03: a snoozed-then-dismissed once-alarm is disabled and not re-armed; a finished dates-alarm is finished"
    - "SNZ-04: snooze past Max Snoozes does not increment and resolves as dismiss; snoozeCount round-trips through JSON"
    - "SNZ-01/SNZ-05: a snoozed alarm survives an unrelated update() still enabled+snoozed, never silently disabled"
    - "The phase's flutter test + analyze gates are COMPLETED in CI (tests.yml on push; test-apk.yml via gh workflow run), with test-apk.yml's analyze scoped to the Phase 2 changed files — not Phase 1's"
  artifacts:
    - path: "test/alarm/types/alarm_snooze_test.dart"
      provides: "Unit regression coverage for the snooze state machine"
      contains: "group('Alarm snooze'"
      min_lines: 80
    - path: ".github/workflows/test-apk.yml"
      provides: "Dispatch-build analyze gate repointed from Phase 1 files to the Phase 2 changed files, so the analyze log covers the snooze fix"
      contains: "lib/alarm/types/alarm.dart"
  key_links:
    - from: "test/alarm/types/alarm_snooze_test.dart"
      to: "Alarm.snooze / Alarm.handleDismiss"
      via: "direct method drive + flag assertions under FLUTTER_TEST + withClock"
      pattern: "withClock|snooze\\(\\)|handleDismiss\\(\\)"
---

<objective>
Author the regression test suite that locks in every Plan-01 snooze fix. There is NO existing snooze unit test (grep confirms snooze is referenced only in `schedule_description_test.dart` and `once_alarm_schedule_test.dart`) — this is the single new file in the phase, composed from three in-repo test analogs (structure / construction / frozen-clock). Tests drive `Alarm.snooze()` / `Alarm.handleDismiss()` directly and assert on `Alarm` flags (`snoozeTime`, `snoozeCount`, `isEnabled`, `isSnoozed`, `isFinished`), NOT on `AndroidAlarmManager` (which no-ops under `FLUTTER_TEST`).

Purpose: Convert the SNZ-01..05 behaviors into committed, CI-runnable proofs — the same pattern Phase 1 used (commit `3e8bd01`) when an on-device check was converted to automated coverage. This guards against regressions in the snooze state machine.
Output: `test/alarm/types/alarm_snooze_test.dart` (new). Plus an end-of-phase on-device smoke check recorded as a `<human-check>` (human-verify mode is end-of-phase — no in-line checkpoint task).

PLANNER NOTE (no CONTEXT.md): this plan depends on Plan 01's API shape — specifically the now-async public `handleDismiss()` (D-E) and the seconds-based `clock.now()` snooze (D-B). The frozen-clock exact assertion for SNZ-02 is only valid BECAUSE Plan 01 switched `snooze()` to `clock.now()`. If Plan 01 had kept `DateTime.now()`, this suite would have to assert a tolerance window instead — it does not, because D-B was taken.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/phases/02-snooze-reliability/02-RESEARCH.md
@.planning/phases/02-snooze-reliability/02-PATTERNS.md
@.planning/phases/02-snooze-reliability/02-01-PLAN.md
</context>

<environment_constraint>
NO Flutter/Dart toolchain exists in the execution env (no `flutter`/`dart` on PATH). The test file may be AUTHORED but NEVER executed locally — do NOT claim a local green run.

These toolchain gates are NOT human-only; they are COMPLETED via GitHub Actions (`gh` is authed on `vicolo-dev/chrono`):
- **`.github/workflows/tests.yml`** runs `flutter test --coverage` automatically on **push to any branch** (no continue-on-error → this is the authoritative test gate; the new `alarm_snooze_test.dart` runs here). Complete it by pushing the branch, then `gh run watch` / `gh run list --branch <branch>`.
- **`.github/workflows/test-apk.yml`** (`gh workflow run test-apk.yml --ref <branch>`) runs `flutter gen-l10n` → `flutter analyze` (scoped, informational) → `flutter test` → builds a RELEASE dev APK with an ephemeral keystore (no org secrets, fork-safe) — the analyze gate plus a sideloadable APK for the human on-device smoke.

Pushing/dispatching targets the upstream `vicolo-dev/chrono` org repo (outward-facing) — confirm before pushing. Frame `flutter test` / `flutter analyze` as CI gates completed via `gh`, never as a local green run.
</environment_constraint>

<tasks>

<task type="auto">
  <name>Task 1: Author alarm_snooze_test.dart covering SNZ-01..05</name>
  <files>test/alarm/types/alarm_snooze_test.dart</files>
  <read_first>
    - test/alarm/types/schedules/once_alarm_schedule_test.dart (STRUCTURE analog — `TestWidgetsFlutterBinding.ensureInitialized()` at top of `main()`, `group`/`setUp`/`test` shape, async tests that drive a domain method then `expect` on flags)
    - test/alarm/widgets/alarm_card_test.dart (CONSTRUCTION analog — :10,:16 build `Alarm(const Time(hour: 2, minute: 30))` with NO storage init; :80 `setSettingWithoutNotify("Label", ...)` mutates a setting in-test; :54 `await sampleAlarm.disable()` drives an async Alarm method)
    - test/alarm/logic/alarm_time.dart (FROZEN-CLOCK analog — :1 `import 'package:clock/clock.dart';`, :12-14 `withClock(Clock.fixed(currentDate), () { ... })`; this pins the time `Alarm.snooze()` now reads via `clock.now()` per Plan-01 D-B)
    - lib/alarm/types/alarm.dart (POST-FIX source from Plan 01 — `snooze()` with the max gate + `clock.now()` + seconds duration; public async `handleDismiss()`; getters `snoozeTime` :76, `snoozeCount` :109, `isEnabled` :68, `isSnoozed` :72, `isFinished` :71; `setSettingWithoutNotify` :175; `toJson`/`fromJson` :440/:392 for the round-trip test; `Alarm.fromJson` builds a fresh instance)
    - lib/alarm/data/alarm_settings_schema.dart (:240-295 the "Snooze" group — settings `Length` and `Max Snoozes` flatten up into the alarm's top-level `_settings`, so `setSettingWithoutNotify("Length", 0.5)` / `setSettingWithoutNotify("Max Snoozes", 2)` resolve directly; "Enabled" defaults true)
    - lib/alarm/logic/schedule_alarm.dart (:28,:101,:136 the FLUTTER_TEST guards — `scheduleAlarm`/`cancelAlarm`/`scheduleSnoozeAlarm` no-op under test, so `snooze()`/`handleDismiss()` run to completion and mutate flags without touching the OS)
    - lib/alarm/types/schedules/dates_alarm_schedule.dart (:47-55,:73-82 — a DatesAlarmSchedule whose only date is today/past finishes (`_isFinished=true`); used by the SNZ-03 dates case. Set the alarm `Type` to `DatesAlarmSchedule` and the `Dates` setting to a past/today date via `setSettingWithoutNotify`)
  </read_first>
  <action>
    Create `test/alarm/types/alarm_snooze_test.dart`. Open `main()` with `TestWidgetsFlutterBinding.ensureInitialized();` (required so the static `appSettings` schema is reachable for `Alarm()` construction — confirmed by the construction analog). Wrap the suite in `group('Alarm snooze', ...)` with a `setUp` that builds a fresh `Alarm(const Time(hour: 2, minute: 30))` per test. Assert on `Alarm` flags only; never assert on `AndroidAlarmManager`.

    Author these cases (the RESEARCH Test Strategy matrix). Use `setSettingWithoutNotify` to configure settings in-test:

    - SNZ-02 (fractional honored, exact under frozen clock): `setSettingWithoutNotify("Length", 0.5)`, then inside `withClock(Clock.fixed(fixedNow), () async { await alarm.snooze(); })` assert `alarm.snoozeTime` equals `fixedNow.add(const Duration(seconds: 30))` exactly (valid because Plan-01 D-B made `snooze()` read `clock.now()`). Also assert `alarm.snoozeTime` is NOT equal to `fixedNow` (never floored to 0). Pick `fixedNow` in the future relative to the alarm time so `scheduleSnoozeAlarm`'s future-date requirement holds even though it no-ops under FLUTTER_TEST.
    - SNZ-03 (once → dismiss deactivates): default once-alarm (`Type` is `OnceAlarmSchedule` by default). `await alarm.snooze();` (assert `alarm.isSnoozed == true`, `alarm.isEnabled == true`), then `await alarm.handleDismiss();` and assert `alarm.isEnabled == false` (or `alarm.isMarkedForDeletion == true` if Delete-After-Ringing were on — it is off by default, so expect disabled), `alarm.isSnoozed == false`, and `alarm.snoozeCount == 0`. This proves the #457 fix: no re-arm, deactivated.
    - SNZ-03 dates (finished-dates → dismiss): set `Type` to `DatesAlarmSchedule` and `Dates` to a single past-or-today date via `setSettingWithoutNotify`; `await alarm.snooze();` then `await alarm.handleDismiss();`; assert the dates schedule is finished/disabled (`alarm.isFinished == true` or `alarm.isEnabled == false`) and `alarm.isSnoozed == false`. Confirms #457 generalizes beyond once-alarms.
    - SNZ-04 (max gate): `setSettingWithoutNotify("Max Snoozes", 2)`; call `await alarm.snooze()` three times. After the 3rd call assert `alarm.snoozeCount` did NOT advance to 3 (it resolved as a dismiss → count reset to 0 per D-A, so assert `snoozeCount == 0` and `isSnoozed == false` and `isEnabled == false`), proving the over-max attempt resolved as a dismiss, never left ringing.
    - SNZ-04 persist (cross-isolate durability): `await alarm.snooze();` (count now 1), then round-trip `Alarm.fromJson(alarm.toJson())` and assert the rebuilt alarm's `snoozeCount == 1`. This proves the count survives serialization = the disk source-of-truth the isolate boundary relies on.
    - SNZ-01/SNZ-05 (snooze survives unrelated update, never silently disabled): `await alarm.snooze();` then `await alarm.update("test: unrelated update while snoozed");` while still before `snoozeTime`; assert `alarm.isEnabled == true` AND `alarm.isSnoozed == true` (the pending snooze is preserved, the alarm is not silently disabled). Optionally add a task-required variant by setting a task on `Tasks` if cheap; the core assertion is "snooze is not destroyed by an unrelated update".

    Match repo conventions: `snake_case` filename, double-quote or single-quote strings consistent with neighbors, `flutter_test` imports, no new dependency. Do NOT modify any `lib/` source in this plan. Do NOT add fixtures or a conftest (Alarm builds without storage init).
  </action>
  <verify>
    <automated>test -f test/alarm/types/alarm_snooze_test.dart</automated>
    <automated>grep -nF "group('Alarm snooze'" test/alarm/types/alarm_snooze_test.dart</automated>
    <automated>grep -nF 'TestWidgetsFlutterBinding.ensureInitialized();' test/alarm/types/alarm_snooze_test.dart</automated>
    <automated>grep -nE "import 'package:clock/clock.dart';" test/alarm/types/alarm_snooze_test.dart</automated>
    <automated>grep -cnE 'snooze\(\)|handleDismiss\(\)' test/alarm/types/alarm_snooze_test.dart</automated>
    <automated>grep -nE 'Max Snoozes|Length|snoozeCount|isSnoozed|isFinished|toJson|fromJson' test/alarm/types/alarm_snooze_test.dart | grep -c .</automated>
  </verify>
  <acceptance_criteria>
    - `test/alarm/types/alarm_snooze_test.dart` exists, opens `main()` with `TestWidgetsFlutterBinding.ensureInitialized();`, and wraps cases in `group('Alarm snooze', ...)`.
    - Contains an SNZ-02 case using `withClock(Clock.fixed(...))` that asserts `snoozeTime == fixedNow + Duration(seconds: 30)` for `Length == 0.5` (and asserts it is not `fixedNow`).
    - Contains an SNZ-03 once-alarm case: snooze then `handleDismiss()`, asserting `isEnabled == false`, `isSnoozed == false`, `snoozeCount == 0`.
    - Contains an SNZ-03 dates case: a `DatesAlarmSchedule` with a past/today date, snooze→dismiss, asserting finished/disabled and not snoozed.
    - Contains an SNZ-04 max case (`Max Snoozes` = 2, three snooze calls) asserting the count does not reach 3 and the over-max attempt resolved as a dismiss.
    - Contains an SNZ-04 persistence case asserting `snoozeCount` round-trips through `toJson`→`fromJson`.
    - Contains an SNZ-01/SNZ-05 case asserting a snoozed alarm survives an unrelated `update()` still `isEnabled && isSnoozed`.
    - Asserts only on `Alarm` flags; no assertion references `AndroidAlarmManager`. No `lib/` file modified. No new dependency added to `pubspec.yaml`.
    - CI gate (completed via `gh`, not locally): pushing the phase branch triggers `tests.yml` → `flutter test --coverage` exits 0 with the new `Alarm snooze` cases green (`gh run watch`); the scoped `flutter analyze` log from `gh workflow run test-apk.yml` shows no new issues for this file.
  </acceptance_criteria>
  <done>`alarm_snooze_test.dart` exists with one CI-runnable case per SNZ-01..05 row (plus the SNZ-04 persistence round-trip), asserting on Alarm flags under FLUTTER_TEST + withClock.</done>
</task>

<task type="auto">
  <name>Task 2: Repoint test-apk.yml's analyze gate to the Phase 2 changed files</name>
  <files>.github/workflows/test-apk.yml</files>
  <read_first>
    - .github/workflows/test-apk.yml (the dispatch build — the "Analyze changed files (informational)" step currently lists Phase 1 files: lib/common/utils/list_storage.dart, paths.dart, json_serialize.dart, lib/common/logic/salvage_report.dart, lib/settings/types/setting_group.dart, lib/system/logic/device_lock.dart, handle_boot.dart, lib/main.dart, lib/app.dart. These are NOT the files Phase 2 changes, so as-is the analyze log would cover Phase 1, not the snooze fix.)
    - .planning/phases/02-snooze-reliability/02-01-PLAN.md (frontmatter `files_modified` — the authoritative list of Phase 2 source files: lib/alarm/types/alarm.dart, lib/alarm/logic/alarm_isolate.dart, lib/alarm/data/alarm_settings_schema.dart)
  </read_first>
  <action>
    Edit ONLY the `flutter analyze \` argument list inside the "Analyze changed files (informational)" step of `.github/workflows/test-apk.yml`. Replace the nine Phase 1 file paths with the four Phase 2 paths:
      lib/alarm/types/alarm.dart
      lib/alarm/logic/alarm_isolate.dart
      lib/alarm/data/alarm_settings_schema.dart
      test/alarm/types/alarm_snooze_test.dart
    Preserve the backslash-continued shell line shape and indentation. KEEP `continue-on-error: true` on the step (deliberate Phase 1 design — alarm.dart is a large pre-existing file that may carry unrelated lints; the step stays informational and the analyze LOG is read for new issues, it does not hard-fail the APK build). Do NOT change the gen-l10n, test, keystore, or build steps. Do NOT change the `on: workflow_dispatch` trigger. Optionally update the stale Phase-1 comment above the analyze step (the "new alarmsResetNotice getter" note) to reflect Phase 2, but this is cosmetic.
  </action>
  <verify>
    <automated>grep -nF 'lib/alarm/types/alarm.dart' .github/workflows/test-apk.yml</automated>
    <automated>grep -nF 'lib/alarm/logic/alarm_isolate.dart' .github/workflows/test-apk.yml</automated>
    <automated>grep -nF 'lib/alarm/data/alarm_settings_schema.dart' .github/workflows/test-apk.yml</automated>
    <automated>grep -nF 'test/alarm/types/alarm_snooze_test.dart' .github/workflows/test-apk.yml</automated>
    <automated>! grep -nF 'lib/common/utils/list_storage.dart' .github/workflows/test-apk.yml</automated>
    <automated>! grep -nF 'lib/system/logic/handle_boot.dart' .github/workflows/test-apk.yml</automated>
    <automated>grep -nF 'continue-on-error: true' .github/workflows/test-apk.yml</automated>
  </verify>
  <acceptance_criteria>
    - The `flutter analyze` step in `test-apk.yml` lists exactly the four Phase 2 paths (alarm.dart, alarm_isolate.dart, alarm_settings_schema.dart, alarm_snooze_test.dart) and none of the Phase 1 paths.
    - The step retains `continue-on-error: true` (informational analyze; the log is read for new issues).
    - The `on: workflow_dispatch` trigger and the gen-l10n / test / keystore / build steps are unchanged.
    - CI gate (completed via `gh`, not locally): `gh workflow run test-apk.yml --ref <phase-branch>` produces a run whose Analyze step log references the Phase 2 files; the build step produces a `chrono-dev-release-apk` artifact.
  </acceptance_criteria>
  <done>test-apk.yml's analyze gate is scoped to the Phase 2 changed files, so a `gh workflow run test-apk.yml` dispatch analyzes the snooze fix (not Phase 1 code) and still emits the sideloadable release APK.</done>
</task>

</tasks>

<verification>
Source-level (locally checkable now):
- `test -f test/alarm/types/alarm_snooze_test.dart` — exists.
- `grep -F "group('Alarm snooze'" ...` and `grep -F 'TestWidgetsFlutterBinding.ensureInitialized();' ...` — present.
- `grep -E "import 'package:clock/clock.dart';" ...` — frozen-clock import present (SNZ-02 exact assertion).
- A case per SNZ row is present (grep for the assertion tokens: `snoozeTime`, `isEnabled`, `isFinished`, `Max Snoozes`, `toJson`/`fromJson`).
- No `lib/` file changed by this plan; no `pubspec.yaml` change.

CI gate (Flutter 3.22.2 — completed via `gh`, cannot run locally):
- `git push` the phase branch → `tests.yml` runs `flutter test --coverage`; confirm green via `gh run watch` / `gh run list --branch <branch>` (authoritative test gate — the new `Alarm snooze` cases run here).
- `gh workflow run test-apk.yml --ref <branch>` → read the scoped `flutter analyze` log (Phase 2 files, informational) and download the `chrono-dev-release-apk` artifact for the on-device smoke below.
- Note: PR runs from a fork show `action_required` (need maintainer approval); a direct push to a branch on the repo auto-runs `tests.yml`. Pushing/dispatching hits the upstream `vicolo-dev/chrono` repo — confirm before pushing.

End-of-phase on-device smoke (human-verify mode = end-of-phase; recorded, not blocking the autonomous tasks):
<human-check>
On a Flutter 3.22.2 device/emulator, `flutter run --flavor dev`:
  1. Create a one-shot ("Once") alarm ~1 min out; let it ring; tap SNOOZE; confirm it re-rings after the configured length (test a fractional Length like 0.5 → ~30s); then on the re-ring tap DISMISS. Relaunch / wait to next day window: the once-alarm must NOT reappear or re-fire (SNZ-03/#457).
  2. Set Max Snoozes low (e.g. 1); snooze up to the max; confirm the snooze button disappears and the alarm cannot exceed the max (and never gets stuck ringing) (SNZ-04).
  3. Confirm a normal snooze re-rings and does NOT silently disable the alarm (SNZ-01/SNZ-05).
Approve when snooze re-rings reliably, fractional length is honored, max is enforced, and a snoozed-then-dismissed once-alarm stays off.
</human-check>
</verification>

<success_criteria>
- A committed `alarm_snooze_test.dart` covers SNZ-01..05 with CI-runnable, device-free assertions on `Alarm` flags.
- SNZ-02 is asserted exactly (now+30s) via the frozen clock enabled by Plan-01's `clock.now()` switch.
- SNZ-03 is asserted for BOTH once-alarms and finished dates-alarms (the #457 generalization).
- SNZ-04 is asserted for the gate (no over-max increment, resolve-as-dismiss) AND for disk persistence (JSON round-trip).
- No `lib/` source change, no new dependency; `flutter test`/`flutter analyze` are CI gates completed via `gh` (push→tests.yml; `gh workflow run test-apk.yml`), never run locally.
- `test-apk.yml`'s analyze gate is repointed to the Phase 2 changed files so a dispatch run analyzes the snooze fix and emits a sideloadable release APK.
</success_criteria>

<output>
Create `.planning/phases/02-snooze-reliability/02-02-SUMMARY.md` when done. Record: the cases authored, the test-apk.yml analyze repoint, and the CI gate completion — the `tests.yml` run id/result for `flutter test` and the `test-apk.yml` run id (analyze log result + APK artifact) once driven via `gh`; if not yet pushed, record them as owed-via-CI with the exact `gh` commands. Record the end-of-phase on-device smoke as the one remaining genuine human gate (consistent with Phase 1's handling).
</output>
